=== ai-data-security build progress (append-only; newest entry last) ===
2026-07-07 | slice 0 | Repo initialized: plugin skeleton (.claude-plugin/), SPEC.md, MIT LICENSE, dev/ harness, stub skills (security-audit orchestrator + forked ai-config-audit). Verifying validate/load/fork mechanics next.
2026-07-07 | slice 0 | DONE. Evidence: dev/validate.sh -> VALIDATE: PASS (claude plugin validate exits 0; one advisory version warning is intentional — git-SHA versioning until public release, --strict deferred to then). Headless run `claude --plugin-dir . -p "/datawarden:security-audit"` returned both DATAWARDEN-STUB lines: orchestrator invoked the context:fork worker via the Skill tool and received its return. S0.1-S0.3 flipped to passes=true.
2026-07-07 | slice 1 | S1.1 + S1.2 DONE. Shared contracts authored against real gitleaks 8.30.1 output (reference/finding-format.md, citations.yml, checks.yml — JSON-in-.yml so jq validates with zero deps). secrets-scanner skill + eval_secrets.py (deterministic exposure matrix, confidence-capped severity, suppression). Evidence: dev/validate.sh VALIDATE: PASS incl. 15 bidirectional fixture assertions (3 generator modes + clean-repo false-positive guard + suppression); live run `claude --plugin-dir . -p "/datawarden:secrets-scanner <fixture>"` rendered a conformant report (SS-03 CRITICAL confirmed + SS-01 HIGH probable, citations resolved, no secret values). Empirical findings: gitleaks allowlists the canonical AWS example key (generator uses runtime-random keys); `gitleaks dir <path>` emits ABSOLUTE File paths while `gitleaks git` emits repo-relative (normalized in eval_secrets.py). S1.3 pending CI green on push.
2026-07-07 | slice 1 | S1.3 DONE. CI run 28895066110 green in 15s: pinned checkout, pinned-checksum gitleaks install, history+worktree self-scans clean, dev/validate.sh full pass on ubuntu. Note for later: GitHub annotation says checkout v4.2.2 targets deprecated Node 20 — bump to a pinned v5 SHA during slice 6 polish. Slice 1 complete.
2026-07-07 | slice 2 | S2.1 DONE. ai-config-audit full skill replaces the stub (context: fork retained): permeval.py deterministic checks AC-01..AC-06 (deny rules, env-runner allows, MCP credential-by-key-name across Claude/Cursor/Gemini/Codex paths, gemini trust:true, transcript INFO, retention always-UNKNOWN), suppression support, ai-config + ai-config-hardened fixtures, 10 new fixture assertions all green. Live run confirmed all 6 checks rendered with AC-06 elevated to first remediation. Harness note: headless `claude -p` runs need `< /dev/null` or the CLI waits on stdin and can return only the invocation trace.
2026-07-07 | slice 3 | S3.1 DONE. Shared reference/four-tier-framework.md (tiers based on SP 800-122 impact levels, filename/column heuristics, content validators) + data-classification skill + classify_hints.py (deterministic floors: never Public, floor Internal; validators upgrade confidence; counts/columns only, never values). classify-repo fixture: 5 assertions green. Live run: 4 files -> 4 distinct tiers incl. README argued Public WITH justification and runbook correctly held at Internal; DC-01 HIGH + DC-02 MEDIUM; leak-check clean. Flakiness note: direct fork-skill invocation via `claude -p` intermittently returns only the fork preamble (~1 in 3 runs) — retry works; investigate before relying on it in CI (we don't).
2026-07-08 | slice 4a | S4a.1 DONE. db-access-audit skill (inline, human-gated) + postgres SQL pack (5 read-only files, psql-var parameterized) + eval_grants.py (DB-01..DB-06; unconfirmed principal caps everything at MEDIUM/possible) + postgres-fixture-check.sh (docker postgres:16-alpine pinned, golden CSV diffs, 9 verdict assertions both confidence modes) wired into run-fixture-checks.sh (self-skips without docker). Live gate tests: WITHOUT --confirm the skill ran ZERO queries, redacted the conninfo password, and asked for dual confirmation (execution + principal identity); WITH --confirm it executed and rendered DB-01..DB-05 with citations and no credential echo. Tightened SKILL.md after test A: never repeat password material even when it looks like an inert fixture value. Host psql via brew libpq (keg-only, /opt/homebrew/opt/libpq/bin).
2026-07-08 | slice 4b | S4b.1 DONE. Snowflake SQL pack (5 read-only files: SHOW + INFORMATION_SCHEMA, snow-CLI -D templating) + recorded fixtures (realistic SHOW output shapes; invented values) + expected_findings.md. New skill capability: --recorded <dir> air-gapped mode (interpret captured outputs without connecting — how locked-down shops can use it, and how these fixtures validate). Live run reproduced expected_findings.md exactly: DB-01 CRITICAL, DB-02 HIGH, DB-03 HIGH+MEDIUM, DB-04 MEDIUM, DB-05 MEDIUM/probable ceiling, fixture-validated framing in header. validate.sh SQL lint covers both packs (zero mutating statements). Nested-claude artifact noted: -p runs occasionally print phantom system-prompt fragments referencing nonexistent memory files — the retry-on-short-output rule stands.
2026-07-08 | slice 5 | S5.1 DONE. security-audit orchestrator replaces the stub: 30-min flow (Phase 0 retention manual-first, then secrets/ai-config/classification forked workers, DB phase only with --db args else skipped-UNKNOWN), merge with dedupe-vs-link overlap table, Top-5 actions severity-first with rotate-first override. make-combined-repo.sh layers every finding class (pushed secret, .env, bad settings/MCP, Confidential+Restricted CSVs). End-to-end run over combined fixtures + recorded Snowflake: all 4 sections + Phase 0, SS-04 rotation ranked #1 above CRITICALs, SS-03+AC-01 linked ("one edit") not merged, DB-02/03/04 clustered into one masked-view action, zero value leakage. Unprompted bonus: report connected AC-03's warehouse credential to the Phase-4 warehouse.
2026-07-08 | slice 6 | S6.1 DONE. README (hero 30-min audit, comparison table dated 2026-07, trust model, roadmap, kclabs.ai links), SECURITY.md (private reporting + explicit trust model + stated limits), CONTRIBUTING.md (fixture doctrine, verified-only citations, harness discipline). CI green incl. tree+history gitleaks self-scans; checkout pinned to v5 SHA. Install verified BOTH ways: local-path marketplace (add/install/details/uninstall clean) AND the real private-git path — root-caused that ~/.gitconfig resets github.com credential helpers to `gh auth git-credential` (work account), so private-phase installs need a one-shot env-appended osxkeychain helper (documented in README; goes away at public release). Earlier CI break root-caused: global gitignore excludes .gemini/ so the fixture needed git add -f. All 12 features pass.
2026-07-08 | review | Adversarial fresh-context review (3 lenses x refuting verifiers, 13 agents): 7 confirmed / 3 refuted. Fixed all 7: (1) glob matcher over-match — '**/`x`' matched 'prod.env', silently downgrading SS-03->SS-02; now '**/' = whole segments, locked by tests/test_matchers.py (12 cases); (2) AC-02 missed colon-wildcard Bash(npx:*) spelling — normalized, uvx:* fixture added; (3) DC-03 was unimplemented — classify_hints.py now emits deterministic DC-01/DC-02 findings + DC-03 unknowns for unreadable files + suppression, new data-classification/reference.md; (4) suppression coverage 2/4 -> 4/4 paths (eval_grants --ignore-dir); (5) malformed expires= now fails CLOSED everywhere; (6) citations.yml pruned of 3 unused entries (prose-cited llm01/t0051-001 kept, rule documented), SPEC parenthetical reconciled; (7) README private-note removal added to CONTRIBUTING release checklist. All harness checks green after fixes.
2026-07-08 | hardening | CI flake root-caused and fixed: (a) exact-count secrets assertion was brittle against gitleaks rule multiplicity on the same planted secret (random-draw dependent) -> replaced with semantic assertions (findings confined to planted files, correct check id per file); (b) run_eval could evaluate a STALE report if a gitleaks invocation errored (|| true swallowed exit>1) -> reports now removed before each run and gitleaks errors retry once then fail loudly; (c) postgres readiness window 30s->90s with container logs on failure. 3x consecutive local harness runs green.
2026-07-08 | hardening-2 | Adversarial edge-case bug hunt (22 agents, reproduce-then-verify): 12 confirmed / rejected the rest. Fixed all 12. BLOCKERS: (1) classify_hints os.walk swallowed untraversable dirs -> onerror callback now emits DC-03 (Restricted subtree can no longer vanish silently); (2) VALUE LEAK — `medical.*` column pattern + header-sniff copied arbitrary first-line free text into pii_columns->evidence; tightened to medical[_a-z0-9]* AND gated pii_columns on a strict identifier regex (<=64 chars). IMPORTANT: eval_secrets relative-target path split (realpath both sides in _relpath + main); blank `expires=` failed OPEN in ALL FOUR load_suppressions -> `if expires is not None` fail-closed everywhere; permeval crashed on non-dict env / non-object top-level JSON / codex config that's a dir-or-bad-UTF8 / permissions:null -> isinstance guards + isfile + wrapped reads; classify FIFO hang -> stat S_ISREG guard -> DC-03; EMAIL ReDoS (20min on 120KB) -> dot-free domain labels, verified linear; UTF-16 BOM text with SSNs filed silent-Internal -> BOM detection + decode; eval_grants malformed CSV KeyError -> per-table required-column validation -> DB-06, + utf-8-sig BOM strip. Regressions locked: test_matchers.py (blank-expiry x4 evaluators + ReDoS timing/match) and a new run-fixture-checks.sh edge block (unreadable subdir, value-leak, FIFO via portable run_bounded, UTF-16, hostile configs, malformed CSV). validate.sh green 3x; self-scan clean; shellcheck clean. Note: one transient SS-04 fail (commit_pushed git subprocess) seen once, 0/4 since — residual git-transient flake, not a code regression.
2026-07-09 | release-prep | Branch release/v0.1.0 stages the public-release checklist steps 1-4 (no publish): plugin.json version 0.1.0 (marketplace.json left version-less so plugin.json wins); CHANGELOG.md; removed README private-phase install note; validate.sh flipped to `claude plugin validate . --strict` and its plugin.json contract check inverted (version now REQUIRED, marketplace entry must NOT carry version). validate.sh VALIDATE: PASS under --strict; gitleaks tree+history self-scan clean after clearing generated/. Step 3 (enable private vulnerability reporting) could NOT be done via API — `gh api -X PUT .../private-vulnerability-reporting` 404s with the personal PAT (missing repo-admin scope / personal-private gating); left as a manual Settings toggle for Kyle. Remaining triggers, all Kyle's: merge this PR, `git tag v0.1.0 && git push origin v0.1.0`, flip repo to public, enable PVR.
2026-07-09 | release-prep | CI flake fixed on the release branch: PR-ref run failed on "postgres never became ready" while the branch-push run passed (same commit) — root cause is the postgres:alpine init-phase temp server, against which pg_isready flickers "ready" before the real server restarts. Replaced the pg_isready gate with a `SELECT 1` query-ready loop (120x1s). Verified 3x locally with fresh initdb each run (forced the restart path). shellcheck clean.
2026-07-09 | v0.1.1 cleanup | Post-launch dogfood fixes. #1: added .claude/settings.json with the recommended secret deny-rules so DataWarden passes its own ai-config-audit (AC-01 clears; only the always-UNKNOWN AC-06 remains). #2: (a) classify_hints filename heuristic now skips source/doc extensions (.py/.md/.sh/...) so a source file merely NAMED with a sensitive word (eval_secrets.py, payment_service.go) is no longer floored by name — content scanning still catches real secrets inside; regression-locked in test_matchers.py. (b) repo .datawarden-ignore triages our own example/fake PII (docs/tests/fixtures) so a self-audit is clean-with-appendix (0 active, 8 suppressed, honest reasons; reasons avoid quoting literal fake values so the ignore file doesn't flag itself). Bumped plugin.json 0.1.0 -> 0.1.1 + CHANGELOG. validate.sh green.
2026-07-09 | v0.1.1 flake fix | The SS-04 CI failure recurred (2nd time) — root-caused empirically: make-secrets-repo.sh generated a RANDOM AWS secret detected only by gitleaks' entropy-based generic-api-key rule, which missed ~1/12 random draws (measured), so intermittently no history finding -> no SS-04. Fixed by switching deploy-creds.txt to a ghp_-format token (github-pat rule is format-based/deterministic, like the .env fixture that never flaked). Verified 0/20 misses on the raw detection and FIXTURES: PASS 3x. This retroactively explains the two earlier one-off SS-04 reds.
2026-07-09 | v0.1.1 flake fix 2 | Different CI failure after the SS-04 fix landed: postgres pack failed on "connection to socket ... No such file or directory". Root cause: the query-ready gate passed against the alpine image's TEMPORARY init-phase server, then the real-server restart pulled the socket out from under the fixture load. Fixed deterministically: gate now requires "ready to accept connections" to appear >=2x in docker logs (temp-init + real server) AND a live SELECT 1 — the count only hits 2 once the real server is up, so it cannot pass early regardless of runner speed. Confirmed a fresh container logs the line exactly 2x; postgres pack green 3x locally. This is a logic fix, not timing-tuning.
2026-07-09 | rename | Kyle chose to rebrand the plugin datawarden -> ai-data-security (matches the series). GitHub: renamed content repo ai-data-security -> ai-data-security-demo (frees the name), then datawarden -> ai-data-security. Local remote set-url'd. Scripted ordered find-replace across 30 files (94 matches): .datawarden-ignore -> .ai-data-security-ignore (file git-mv'd too), datawarden.local.md -> ai-data-security.local.md, /datawarden: -> /ai-data-security:, DataWarden -> "AI Data Security", datawarden -> ai-data-security. feature_list DATAWARDEN-STUB -> AI-DATA-SECURITY-STUB; progress.txt header updated, dated entries kept as history. Version 0.1.1 -> 0.1.2 + CHANGELOG "Changed" entry with reinstall steps. Verified: validate.sh --strict PASS, matcher+fixtures+postgres green, gitleaks clean, and a LIVE `/ai-data-security:secrets-scanner` run rendered the correct 2 findings under the new prefix. Local dir rename + re-release + demo cross-link next.
2026-07-15 | slice 7 | v0.2 features S7.1-S7.3 built and demonstrated (dev/validate.sh + run-fixture-checks.sh green locally; docker unavailable so the postgres golden diff runs in CI). S7.1 org profile: .ai-data-security.yml (JSON-formatted, citations.yml precedent, stdlib-only) auto-discovered at target root by classify_hints and next to --ignore-dir by eval_grants; extends-only (tokens validated against ^[A-Za-z0-9_-]{1,64}$ so config can't inject regex), unparseable -> DC-03/DB-06 with "extensions were NOT applied". New reference/org-config.md. S7.2 token-boundary PII: anchored ^...$ column patterns missed customer_email/member_ssn/borrower_dob; now (^|[_ ])(tok)([_ ]|$) in classify_hints (search not match — match() would have silently kept anchoring) and (^|_)(tok)(_|$) in both SQL packs (Snowflake REGEXP_LIKE is implicitly anchored, hence .*...* wrapping); emailed_at proven NOT to match; postgres fixture gains member_ssn/customer_email/emailed_at, golden CSV hand-computed and cross-checked by simulating the regex in python. SQL packs gain org_restricted/org_confidential vars with (^|_)(__none__)(_|$) no-op defaults (postgres-fixture-check.sh passes them). S7.3 snowflake script-backing: eval_grants --dialect snowflake parses recorded snow-sql ASCII tables (multi-statement files, comment/trailer skipping, len-mismatch rows dropped) and computes DB-01..DB-05 incl. OWNERSHIP as a write priv, name-suffix matching for readable tables, DB-05 capped at probable (partly organizational); output matches expected_findings.md exactly; garbage input fails closed to DB-06. eval_grants tools version 1->2, dialect now honest. Feature list $comment amended to allow APPENDING new-slice entries (v1 entries untouched).
2026-09-11 | slice 8 | v0.3.0 built from the research pass (docs/research, PR #4): doctor + quick-check skills (reuse evaluators, no new verdict engine), AC-01 matches deny spellings by meaning (0.2.0 string-compare false-positive on Read(.env) regression-locked), AC-07 sandbox posture, citation provenance fields + CI check + OWASP Agentic 2026/cheat-sheet/MCP-dated entries, SARIF exporter, README Mission/Vision + category positioning, SPEC amendment for the read-only safe-db-access planner, ROADMAP. Evidence: dev/validate.sh PASS locally (postgres pack self-skipped, no docker); quick-check on the combined fixture 0.2s with 3 verdicts; doctor exits 0 with missing home; Codex review gates per portion recorded under docs/research/codex-run/ and the PR.
2026-09-12 | slice 9 | v0.4.0 decision-quality DB audit: DB-ID-01 (effective identity), DB-07 (indirect boundary paths), DB-08 (policy attachment; dynamic-table owner INFO), DB-09 (audit-trail quality incl. result cache), DB-10 (external paths) on both dialects; four new Postgres pack files + three Snowflake files; evaluator inputs optional and fail-closed to DB-06 with the capture statement and the GOVERNANCE_VIEWER precondition. Postgres goldens regenerated in docker (fixture gains inherited group, PUBLIC grant, default privileges, pg_write_server_files). Evidence: postgres-fixture-check.sh PASS locally in docker; run-fixture-checks sf/pg assertions green; validate.sh PASS.
2026-09-12 | slice 10 | v0.5.0 agent-config depth + dbt static audit: AC-08 capability inventory (INFO, never a detection), AC-09 MCP provenance (unpinned npx/uvx/bunx/pipx/pnpm dlx, @latest, git URLs), AC-10 write-capable warehouse MCP (Snowflake sql_statement_permissions read from the YAML named by --service-config-file, Postgres MCP Pro --access-mode, Toolbox tools.yaml statements; missing/hostile config -> UNKNOWN), AC-11 local sinks (DuckDB stored_secrets, connections.toml/.databrickscfg/profiles.yml/.aws/credentials/.pgpass key names only, shell history w/o deny); six more agent-tool config paths (VS Code servers+headers, Windsurf, Cline, Roo, Continue, Copilot CLI, OpenCode). classify_hints gains IBAN mod-97 / phone / IPv4 validators (classify-repo goldens unchanged). New skill dbt-governance-audit (stdlib YAML-subset reader; DBT-01..04; every finding repeats "a missing tag is not proof of no PII"; no dbt_project.yml -> UNKNOWN). Wired into doctor + security-audit phase 3b. Evidence: validate.sh PASS in docker (postgres pack), run-fixture-checks new steps green incl. AC-10 fail-closed and dbt-missing steps, test_matchers v0.5 cases, hardened fixture still zero findings, no placeholder value string in any output. Codex gate (docs/research/codex-run/v0.5-gates.md) found 9 issues, all fixed pre-merge: OpenCode mcp.servers nesting, Continue YAML paths, Snowflake Unknown:True as write, remote Postgres MCP -> UNKNOWN, CTE writes + unclassifiable Toolbox statements, shared hardened YAML reader (scripts/yaml_subset.py), per-model masking declarations instead of a name heuristic, pipx/== /SHA pins, versioned refs.
2026-09-12 | slice 11 | v0.6.0 safe-db-access planner (approved SPEC amendment): plan.py renders fixed templates (skills/safe-db-access/templates/{postgres,snowflake}) with IDENT-validated identifiers from eval_grants' new value-free plan_inputs block (tool 3->4; columns.sql added to the pg pack for explicit view columns). Seven sections: identity, vault (HMAC/SHA-256 key outside the AI role's reach), curated views (Restricted omitted, Confidential hashed; sf secure views + IS_AGENT_ACTIVATED() masking policy), grants, audit, validation-as-AI-role, rollback. Refusals: non-identifier params exit 2 with empty stdout; hostile warehouse names -> NOT RENDERED; missing columns -> INCOMPLETE; unfilled slot -> exit 3. CI: postgres-fixture-check applies the plan, validates as ai_agent (all ok=t), raw read refused, re-audit shows DB-01/02/03/04/07/10/ID-01 gone, rollback restores pack goldens. validate.sh lints plan.py for connect/execute/write primitives. Codex gate (docs/research/codex-run/v0.6-gates.md): 12 findings, 11 fixed pre-merge (P0: vault/curated schema could equal a raw schema and be DROPped on rollback), 1 accepted (no live Snowflake harness; validation rewritten as succeed-and-compare statements). Pseudonymized tier moved from name-only (pack) to provenance (evaluator: suffix AND hashing view).
2026-09-13 | slice 12 | v0.7.0 coverage begins: Databricks Unity Catalog pack (sql/databricks, 7 files + README) and the first evaluator dialect MODULE (scripts/dialects/databricks.py; eval_grants --dialect databricks --recorded <dir>); identity by shape (UUID=service principal, email=user, group), group + inherited + 'account users' paths named per grant, DB-08 via COLUMN_MASKS, DB-10 external locations/credentials/volumes, permanent DB-06 for the INFORMATION_SCHEMA own-grants precondition. Snowflake carry-over: identity.sql statement 3 (CURRENT_USER/ROLE/SECONDARY_ROLES) compared to --role. Five Databricks doc pages fetched live for citations. Planner refuses non-pg/sf dialects plainly. Codex gate (docs/research/codex-run/v0.7-gates.md): 9 findings, all fixed pre-merge — broken $(cat) capture pipeline, WITH USER vs WITH GROUP, schema/catalog ownership, MANAGE as control-plane reach, cascading volume privileges, USE CATALOG/USE SCHEMA effective-access rule (latent grants LOW), casefolded matching, invisible view definitions -> UNKNOWN, CURRENT_SECONDARY_ROLES JSON parsing.
2026-09-13 | slice 13 | v0.8.0 Redshift pack (sql/redshift, 9 files) + scripts/dialects/redshift.py: identity closure (svv_user_grants -> svv_role_grants walk, pg_group, PUBLIC), schema-USAGE effective-access rule (latent LOW), scoped TABLES grants, casefolded ids, auditor standing recorded so empty secadmin-only policy views fail closed to DB-06, IAM:/IAMA: prefix as the IAM-auth signal, enable_user_activity_logging for DB-05, external schemas for DB-10 with a permanent UNLOAD/COPY DB-06. Eight AWS doc pages fetched live for citations (POSIX ~ is case-sensitive -> LOWER(); pg_views not documented -> pg_class + pg_get_viewdef). Codex gate (docs/research/codex-run/v0.8-gates.md): 9 findings, all fixed pre-merge — output_columns (not input) for DDM coverage, superuser-only grant visibility -> partial DB-06 and 'not a clean result', SVV_IAM_PRIVILEGES for UNLOAD/COPY, ownership as write authority, SVV_COLUMN_PRIVILEGES, enable_case_sensitive_identifier-aware comparison, SVV_DATABASE_PRIVILEGES scoped grants, DB-04 UNKNOWN below secadmin, USAGE-only external reach. Pack is now 12 files.
2026-09-13 | slice 14 | v0.9.0 BigQuery pack (partial by design): sql/bigquery (grants_dataset + grants_table because OBJECT_PRIVILEGES must be filtered to one object; pii_columns; masked_views; audit_logging over JOBS) + JSON captures (iam_policy, sa_keys, log_bucket, row_access_policies) + policy_tags.csv from bq show --schema. scripts/dialects/bigquery.py maps IAM roles to capabilities, applies project-level roles to every dataset (the inherited access the view hides), dataset-level to every table, everyone principals, latent without jobUser; DB-08 joins policy tags; permanent DB-06s for groups, custom roles, conditions, taxonomy IAM. Loader gains JSON_INPUTS. Google docs pages return nav-only to the fetcher; facts verified via search snippets of the official pages + two fetched pages. Codex gate (docs/research/codex-run/v0.9-gates.md): 12 findings, 11 fixed + authorized views as fail-closed UNKNOWN — basic roles are not table access, conditional bindings excluded, deny policies captured, indirect grantees -> DB-06, manifest-checked capture loop, per-principal cache hits, COLUMN_FIELD_PATHS for nested PII, data policies recognised, FGR inference removed, optional sa_keys, jobs.create precondition.
2026-09-13 | slice 15 | v0.10.0 Fabric Warehouse pack (sql/fabric, 6 T-SQL files each emitting its own header for sqlcmd -h -1 CSV) + scripts/dialects/fabric.py: principals (E/X types), transitive role membership, fixed roles as authority, DENY > GRANT and column > object precedence, DDM per PII column, RLS policies, Query Insights, optional audit_status.json (REST settings/sqlAudit) -> DB-05 DISABLED/enabled/unknown; permanent DB-06 that workspace roles (even Viewer = ReadData) and item permissions are invisible from T-SQL. Ten Microsoft Learn pages fetched live. Lesson from v0.9: run gitleaks on git HISTORY after committing (a 40-hex fixture key id tripped generic-api-key in CI; amended + force-pushed). Codex gate (docs/research/codex-run/v0.10-gates.md): 10 findings, all fixed — Database Engine precedence (column GRANT over object DENY; CONTROL implies SELECT, ALTER does not), UNMASK/CONTROL/ownership make masks non-binding, ownership.sql, modules.sql (EXECUTE = opaque read path), endpoint_mode.json (user-identity mode -> table verdicts UNKNOWN), audit predicate/actions, QUOTENAME CSV quoting, stricter principal validation, strict audit state.
