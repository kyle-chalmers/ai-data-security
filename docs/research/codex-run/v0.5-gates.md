# Codex review gate — v0.5.0 diff (2026-09-12)

Independent adversarial review of the v0.5 diff (`git diff origin/main -- skills reference tests scripts CHANGELOG.md`, 93 KB) by Codex CLI (`codex exec`, read-only sandbox, live web search), against SPEC.md and finding-format.md. Findings are leads; each was verified before disposition. Nine issues, all accepted and fixed in the same PR.

## Disposition

| # | Codex finding | Sev | Verified? | Disposition |
|---|---|---|---|---|
| 1 | OpenCode nests servers at `mcp.servers`; scanner read the flat `mcp` map | P1 | Yes (opencode.ai schema) | Fixed: `_servers_at` resolves `mcp.servers`, falls back to flat `mcp`; fixture `opencode.json` + AC-03 assertion |
| 2 | Continue's current config is `~/.continue/config.yaml` + `.continue/mcpServers/*` (YAML list); only legacy `config.json` was scanned | P1 | Yes (docs.continue.dev) | Fixed: YAML sources read via the shared reader; unreadable file → AC-03 UNKNOWN; fixture `.continue/mcpServers/analytics.yaml` (AC-09 + AC-03) |
| 3 | Snowflake `Unknown: True` passes unmapped statements but was not in `SF_WRITE_TYPES` | P1 | Yes (Snowflake-Labs/mcp README) | Fixed: `unknown` is write-capable; remediation lists it; fixture flips `Unknown: True`, assertion checks the evidence list |
| 4 | Remote (URL/SSE) Postgres MCP Pro has no local `--access-mode`; code emitted confirmed HIGH | P1 | Yes (crystaldba/postgres-mcp) | Fixed: URL-based or command-less server → AC-10 UNKNOWN; fixture `postgres-mcp-remote` |
| 5 | Toolbox write detection only matched a leading DML token; a `WITH … (DELETE … RETURNING) SELECT` CTE passed as clean; leading comments unhandled | P1 | Yes | Fixed: `classify_statement` strips comments, treats `WITH` containing a write keyword as write, reports unclassifiable statements as AC-10 UNKNOWN; fixture tools `archive-rows`, `weekly-summary`, `set-path` |
| 6 | dbt YAML reader accepted unsupported constructs as strings (`meta: {contains_pii: true}` not a dict; `models: [` accepted) → DBT-01 false negatives without UNKNOWN | P1 | Yes | Fixed: one shared `scripts/yaml_subset.py` for both evaluators; single-level flow maps and block scalars supported; anchors/aliases/tags, nested flow collections, unmatched quotes/brackets, multi-document, tabs, mixed blocks → ValueError → UNKNOWN. Unit tests cover hostile inputs (ValueError, never a crash) |
| 7 | One unrelated model named like `mask`/`curated` suppressed DBT-01 project-wide with no lineage | P1 | Yes | Fixed: masking honored only per model (`meta.masked` & co.) or as a package; name matches are reported as hints in evidence/summary, never as proof; fixture `customer_masked` (declared) vs `customer_curated` (hint only) |
| 8 | AC-09 treated `pipx run` as the package and flagged git URLs pinned with `#<sha>` | P2 | Yes | Fixed: `pipx run`, `pkg==x.y.z`, `uvx --from`, full-SHA fragments; unit cases added |
| 9 | DBT-01 only parsed one-argument `ref('m')`; versioned / package-qualified refs evaded | P2 | Yes (dbt docs) | Fixed: `depends_on_model` handles `ref('pkg','m')`, `ref('m', version=N)`, `source('s','t')`; unrecognized forms → DBT-03 UNKNOWN naming the exposure; fixture `legacy_report` |

Codex's "correct parts" (IBAN mod-97, VS Code `servers`/`headers`, DuckDB secret location and no encryption, Snowflake service-config shape, stdlib-only, no value printing) stand.

## Evidence after fixes
- `tests/test_matchers.py` PASS (adds `_unpinned` ×16, `classify_statement` ×8, YAML shape + 10 hostile inputs, IBAN, `depends_on_model` ×9).
- `tests/run-fixture-checks.sh` PASS; `./dev/validate.sh` PASS (docker Postgres pack included).

## Raw output

```
Found 9 concrete issues. No files were changed.

| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | `permeval.py:473-485` | OpenCode v2 nests servers at `mcp.servers`, but the scanner treats `mcp` itself as the server map. It therefore misses AC-03/08/09/10 for every current OpenCode config. [OpenCode’s schema](https://opencode.ai/v2/docs/mcp-servers) documents `mcp.servers`. | P1 | Resolve OpenCode servers as `data["mcp"]["servers"]`, while retaining the legacy flat form if supported. |
| 2 | `permeval.py:471` | Continue’s current supported MCP locations are `~/.continue/config.yaml` and workspace `.continue/mcpServers/*` YAML/JSON files; scanning only legacy `~/.continue/config.json` silently misses configured servers. [Continue docs](https://docs.continue.dev/customize/deep-dives/mcp) | P1 | Scan the documented YAML config and each `.continue/mcpServers/` entry with the safe YAML-subset reader; emit UNKNOWN for unreadable/unsupported entries. |
| 3 | `permeval.py:529-530,628-631` | Snowflake `Unknown: true` permits SQL expressions the mapper does not recognize, including future/unmapped write statements, but `unknown` is absent from `SF_WRITE_TYPES`. AC-10 can therefore pass a write-capable configuration. [Snowflake-Labs documents that `Unknown: True` allows uncaptured commands](https://github.com/Snowflake-Labs/mcp#sql-execution). | P1 | Treat `Unknown: true` as write-capable (or at minimum AC-10 UNKNOWN); include it in the remediation’s statement-type list. |
| 4 | `permeval.py:642-649` | A remote Postgres MCP Pro server (`type: sse`/URL) has no local `--access-mode` argument to inspect. The code nevertheless emits a **confirmed HIGH** claiming the remote server is unrestricted. The server’s own docs show this remote configuration form. [Postgres MCP Pro](https://github.com/crystaldba/postgres-mcp) | P1 | If the server is URL-based or has no local launch command, emit AC-10 UNKNOWN: its server-side access mode is not locally auditable. Reserve confirmed HIGH for an observed local unrestricted/absent mode. |
| 5 | `permeval.py:531,672-674` | Toolbox write detection only matches SQL whose first token is DML/DDL. A data-modifying CTE such as `WITH removed AS (DELETE … RETURNING *) SELECT * FROM removed` executes a delete but is reported clean. | P1 | Parse/flag leading `WITH` statements containing write keywords, and either handle leading SQL comments or emit UNKNOWN instead of passing statements the limited matcher cannot classify. |
| 6 | `dbt_audit.py:52-62,250-258` | The YAML reader silently accepts unsupported or malformed constructs as scalar strings. For example, valid `meta: {contains_pii: true}` is not a dict, so tags are ignored; malformed `models: [` is also accepted rather than producing DBT-03. This yields DBT-01 false negatives and DBT-02 false positives without the required UNKNOWN. | P1 | Reject unsupported flow mappings, anchors, malformed flow syntax, and unmatched quotes with `ValueError`; alternatively implement the needed subset. Ensure every unsupported file produces DBT-03 UNKNOWN. |
| 7 | `dbt_audit.py:304-306,334` | One unrelated model whose name contains `mask`, `curated`, or similar suppresses DBT-01 for every PII exposure in the project. There is no lineage check that the exposure actually consumes that masking layer. | P1 | Determine masking per consumed model/exposure from explicit metadata or dependency lineage; do not use a project-global name heuristic as proof of masking. |
| 8 | `permeval.py:534-556` | AC-09 does not understand `pipx run package==x.y.z` and treats `run` as the package, so pinned pipx invocations are falsely reported. It also flags Git URLs pinned with the normal `#<commit>` fragment because it only looks for `@`. | P2 | Parse runner-specific syntaxes: skip `pipx run`, accept exact `==` versions, and accept immutable full-SHA Git fragments while rejecting branches/tags/ranges. |
| 9 | `dbt_audit.py:324` | DBT-01 only recognizes one-argument `ref('model')`. dbt exposures can use versioned or package-qualified refs, such as `ref('dim_customers', version=2)`; these are not normalized and evade the exposure check. [dbt’s versioned-ref example](https://github.com/dbt-labs/dbt-core/discussions/6736) | P2 | Parse the first positional model argument from supported `ref()` forms, or mark complex/unrecognized `depends_on` entries UNKNOWN rather than silently ignoring them. |

Correct parts:

- The IBAN implementation follows the expected ISO 13616 mod-97 rearrangement/check approach. [ISO 13616-1](https://www.iso.org/obp/ui?_escaped_fragment_=iso:std:iso:13616:-1:ed-2:v1:en)
- VS Code’s `.vscode/mcp.json` `servers` key and support for `headers` are correctly targeted. [VS Code MCP schema](https://code.visualstudio.com/docs/agents/reference/mcp-configuration)
- DuckDB’s default persistent-secret location and lack of encryption are accurately described. [DuckDB documentation](https://duckdb.org/docs/stable/configuration/secrets_manager.html)
- The Snowflake service-config argument and basic `sql_statement_permissions` list shape are correctly identified.
- The new scripts remain stdlib-only, and the new evidence paths generally avoid printing credential/data values.```
