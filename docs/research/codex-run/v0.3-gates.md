# Codex review gates for v0.3.0

Per the standing directive, Codex (`codex exec -s read-only`) reviewed each portion of the release
before it was committed. Raw reviews are appended below each disposition table.

## Gate 1 — portions 1 and 2 (docs, SPEC amendment, AC-01 semantics, AC-07, citation provenance)

| # | Severity | Disposition | What changed |
|---|---|---|---|
| 1 | P1 | **Accepted** | `deny_rule_target` no longer trims inside the parentheses; `Read( .env )` returns None. Negative tests added |
| 2 | P2 | **Accepted** | Coverage is now glob subsumption over representative probe paths, not string equality: `Read(.env*)` satisfies both `.env` and `.env.*`; `Read(secrets/*)` does not satisfy `secrets/**` (misses nested files). Tests added |
| 3 | P1 | **Accepted** | AC-07 requires `sandbox.enabled` **and** not `sandbox.filesystem.disabled`; enabled without `failIfUnavailable` is a separate INFO finding. Title now says "filesystem isolation". Fixtures for both variants added |
| 4 | P1 | **Accepted** | SPEC v0.3 section restates the invariant honestly: byte-identical for existing checks; a new check (AC-07) may add a finding to a no-config repo and the CHANGELOG names it |
| 5 | P1 | **Rejected (timing)** | `/doctor`, `/quick-check`, and `--sarif` were untracked at review time and ship in the same release commit (portions 3 and 4, gate 2). README and SPEC describe shipped functionality |
| 6 | P1 | **Rejected (timing)** | Same as #5 |
| 7 | P2 | **Accepted** | Provenance check now requires non-empty **string** `published`/`accessed`, and fails any citation that is neither referenced by a check nor in the prose-only allowlist (`owasp-llm01`, `atlas-aml-t0051-001`) |

Correct-as-is confirmations from the reviewer: AC-01 rejects `Read(env)`, `Read(.envrc)`, `Read(**/.env/**)`, `Edit(.env)`, rules in `ask`; AC-07 survives `sandbox: null`, `[]`, `"true"`; the jq checks fail on omitted fields and withdrawn citations; all new citations are referenced.

### Raw review (gate 1)

| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | `skills/ai-config-audit/scripts/permeval.py:47` | `pattern.strip()` accepts undocumented `Read( .env )` as `Read(.env)`. A rule whose path literally contains spaces does not protect `.env`, so AC-01 can incorrectly PASS an unprotected project. | P1 | Do not trim inside the parentheses; accept only documented spellings, or validate the rule with the same path-matching semantics used for actual permission coverage. Add a negative test for `Read( .env )`. |
| 2 | `skills/ai-config-audit/scripts/permeval.py:55` | Equality matching rejects stronger valid globs such as `Read(.env*)`. Under gitignore-style matching, this covers both `.env` and `.env.*`, but AC-01 reports both missing. | P2 | Test rule coverage/subsumption, not normalized-string equality. Add `Read(.env*)` and `Read(**/.env*)` fixtures. |
| 3 | `skills/ai-config-audit/scripts/permeval.py:160` | AC-07 clears solely on `sandbox.enabled is True`. It therefore clears when `sandbox.filesystem.disabled: true`, which removes the filesystem boundary the finding claims to assess; it also ignores `failIfUnavailable` and `allowUnsandboxedCommands`, allowing fallback or retry outside the sandbox. | P1 | Either require filesystem isolation plus hard-gate settings for a clear verdict, or rename/split the verdict to say only that sandboxing is configured. Add explicit fixtures for `filesystem.disabled`, unavailable sandbox, and unsandboxed retry. |
| 4 | `SPEC.md:100` | The claimed preserved invariant, “zero-config byte-identical,” is contradicted by AC-07: a repo/home with no sandbox config now receives a new confirmed MEDIUM finding. The new test explicitly proves this behavior. | P1 | Amend the invariant for v0.3, or make no-config AC-07 an UNKNOWN / preserve prior output semantics. Keep SPEC and ROADMAP consistent. |
| 5 | `README.md:45` | README advertises `/doctor`, `/quick-check`, and `security-audit --sarif` as currently usable, but those implementations are untracked and absent from the reviewed diff/main. This portion would ship broken commands. | P1 | Label these as forthcoming in a later v0.3 portion, or defer the README claims until those portions are shipped. |
| 6 | `SPEC.md:104` | SPEC states doctor, quick-check, and SARIF as completed “v0.3 additions,” although they are not in this reviewed portion. It creates the same release-scope overclaim as the README. | P1 | Mark them planned/pending until their implementation is part of the release, then update the completion wording. |
| 7 | `tests/run-fixture-checks.sh:39` | The provenance gate does correctly reject a missing field and a withdrawn citation used by a check, but it does not enforce `citations.yml`’s own “every entry is used by a check or shipped prose” rule. It also accepts non-string provenance values such as `"published": []`. | P2 | Add an explicit unused-citation allowlist/mapping for prose-only IDs and fail all other unused entries; require non-empty strings for `published` and `accessed`. |

Correct as-is:

- AC-01 correctly rejects `Read(env)`, `Read(.envrc)`, `Read(**/.env/**)`, `Edit(.env)`, and rules in `ask`; case and trailing-slash variants are conservatively flagged, not falsely passed.
- The documented `Read(.env)`, `Read(**/.env)`, `Read(./.env)`, and `Read(//**/.env)` spellings work.
- AC-07 safely handles `sandbox: null`, `[]`, `"true"`, and `enabled: "true"` without crashing or clearing the finding.
- The jq checks do fail on an omitted provenance field and, with `-e`, on a withdrawn citation referenced by `checks.yml`.
- All newly added citations are referenced by `checks.yml`; the two registry entries not used by checks are the existing prose-only `owasp-llm01` and `atlas-aml-t0051-001`.

The sandbox concern is substantive: filesystem isolation can be disabled independently, and hard enforcement requires additional settings beyond `enabled`. [Claude Code sandbox docs](https://code.claude.com/docs/en/sandboxing)  

I could not complete the full fixture script in this read-only environment because `mktemp` is blocked; its two registry checks did run and pass.
## Gate 2 — portions 3 and 4 (doctor, quick-check, SARIF, release notes)

| # | Severity | Disposition | What changed |
|---|---|---|---|
| 1 | P0 | **Accepted** | `doctor.probe` no longer renders child output; it keeps only a `major.minor[.patch]` token matched by a strict regex. Fixture: a PATH-shadowed `psql` printing a secret-shaped line yields version `99.9.9` and nothing else |
| 2 | P0 | **Accepted / partly rejected** | `run_json` returns fixed error categories (exit code, timeout, unreadable JSON, unexpected shape); child stdout/stderr are never surfaced. The target path stays in the output: it is the audit subject and every evaluator's JSON already carries `target` |
| 3 | P0 | **Rejected** | `--emit-json` is the documented contract of every evaluator (SPEC design rule 1: "Read-only except the optional --emit-json path") and the fixture harness depends on it. Write failures are now caught and reported inside the result instead of crashing |
| 4 | P0 | **Accepted** | Suppression `reason` is no longer copied to SARIF; justification is a fixed string pointing at the ignore file. Docstring and CHANGELOG say so. Fixture proves a reason value never appears |
| 5 | P1 | **Accepted** | `doctor` catches `ValueError` (covers Unicode and JSON errors), malformed registry shapes, and `--emit-json` write errors; a top-level guard renders a degraded result and still exits 0 |
| 6 | P1 | **Accepted** | `_valid_blob` validates evaluator output shape (object; findings/unknowns/suppressed are lists of objects); violations become lane UNKNOWNs. Binary output handled via `errors="replace"` and `ValueError` |
| 7 | P1 | **Accepted** | Verdict lines report suppressed findings as suppressed ("deny-rule finding suppressed (AC-01)", "sandbox finding suppressed", secret/classification suppressed counts), never as a pass. Fixture added |
| 8 | P1 | **Accepted** | SS-01 "history not scanned" is appended unconditionally before the gitleaks probe |
| 9 | P1 | **Accepted** | Suppressed-fingerprint parsing uses `partition`/`rpartition`, preserving objects that contain `:`; locations route by skill (`db-access-audit` → logicalLocations), not punctuation. Fixture added |
| 10 | P2 | **Accepted** | Fingerprints coerced to strings; artifact URIs percent-encoded; base URI is `file://` + encoded target + `/`; malformed blobs/findings skipped. Junk-input fixture added |
| 11 | P2 | **Accepted** | `doctor` and SARIF tests now run before the gitleaks gate with a PATH mock (gitleaks absent → secrets-scanner UNKNOWN); the timing bound is now deterministic (3 lanes × 35 s < 120 s) |
| 12 | P2 | **Accepted** | Focused SARIF fixtures for colon fingerprints, reason withholding, logical locations, malformed input |
| 13 | P1 | **Accepted** | CHANGELOG wording corrected (suppression reasons withheld; lane budget; stderr never surfaced) |

Also noted, no change: `security-audit` has no `allowed-tools` restriction by design (it must invoke the Skill tool); the new SARIF step uses `python3` like every other step.

### Raw review (gate 2)

| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | [doctor.py:45](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/doctor/scripts/doctor.py:45) | `probe()` captures an arbitrary executable’s first stdout/stderr line as its “version,” then renders it. A PATH-shadowed `gitleaks`/other tool can print a secret or config value. | P0 | Do not emit child output; report only presence, or validate a narrowly safe version token before retaining it. |
| 2 | [quick_check.py:45](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/quick-check/scripts/quick_check.py:45) | On evaluator failure, `run_json()` copies the last raw child stdout/stderr line into an UNKNOWN which is printed at line 227. That defeats the redaction boundary; it also prints the unredacted target path at line 220. | P0 | Replace child output and exception text with fixed error categories/exit codes; never render target paths unless explicitly treated as safe metadata. |
| 3 | [doctor.py:197](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/doctor/scripts/doctor.py:197), [quick_check.py:213](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/quick-check/scripts/quick_check.py:213) | Both public scripts accept `--emit-json` and write to any caller-selected path, contrary to the “no writes outside a temp dir” constraint. | P0 | Remove the external write option from these scripts; emit JSON on stdout or keep intermediates in their owned temp directory. |
| 4 | [to_sarif.py:94](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/scripts/to_sarif.py:94) | Suppression `reason` is free text from `.ai-data-security-ignore`, not a redacted evaluator field, and is copied verbatim into SARIF `justification`. It can contain a secret/data/config value, invalidating “shareable.” | P0 | Omit free-text reasons from SARIF, or enforce a conservative redaction/allowlist before serialization. Update the shareability claim. |
| 5 | [doctor.py:45](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/doctor/scripts/doctor.py:45), [doctor.py:103](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/doctor/scripts/doctor.py:103) | “Always exits 0” is not true for binary tool output or binary home settings (`UnicodeDecodeError` is uncaught), malformed registry shapes, and failing `--emit-json` writes. | P1 | Catch `UnicodeDecodeError`, `ValueError`, and shape/type errors at the top level; emit a fixed degraded result and return 0. |
| 6 | [quick_check.py:47](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/quick-check/scripts/quick_check.py:47) | Non-JSON is handled, but binary output is not (`UnicodeDecodeError`), and valid-but-wrong JSON shapes subsequently crash on `.get()`/iteration. This fails the hostile evaluator-output contract. | P1 | Validate each evaluator blob is an object and each collection contains objects with expected scalar fields; treat all violations as lane UNKNOWNs. |
| 7 | [quick_check.py:121](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/quick-check/scripts/quick_check.py:121) | The three condensed verdicts are false when findings are suppressed: zero active SS-03 yields “no secrets found,” and suppressed AC-01/AC-07 yields “deny rules present”/“sandbox on.” | P1 | Account for suppressed findings explicitly: say the state is suppressed/not established and include relevant suppressed counts instead of inferring a pass. |
| 8 | [quick_check.py:56](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/quick-check/scripts/quick_check.py:56) | SS-01 (“history not scanned”) is honest when the working-tree lane succeeds, but is omitted if gitleaks is missing/fails—even though history still was not scanned. Thus this is not the promised full UNKNOWN list. | P1 | Add the SS-01 UNKNOWN unconditionally before attempting the directory scan. |
| 9 | [to_sarif.py:57](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/scripts/to_sarif.py:57), [to_sarif.py:109](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/scripts/to_sarif.py:109) | DB objects containing `:` are misparsed for suppressed results: `DB-03:schema.table:column:db` becomes file `schema.table`, dropping `column`; the dot heuristic also emits normal warehouse objects as physical file locations rather than logical locations. | P1 | Parse fingerprint as check-id + rightmost qualifier (`split(':', 1)` then `rsplit(':', 1)`), preserve the full object, and route DB/object findings to `logicalLocations` by finding type rather than punctuation. |
| 10 | [to_sarif.py:82](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/scripts/to_sarif.py:82), [to_sarif.py:133](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/scripts/to_sarif.py:133) | Normal evaluator output satisfies the requested SARIF basics, but the converter does not enforce string fingerprints or URI-safe/relative artifact paths. A malformed interchange blob can produce an invalid non-string partial fingerprint; targets with spaces are emitted as unescaped URI bases. | P2 | Validate/coerce schema fields, percent-encode URI references, and reject or normalize absolute artifact paths relative to `TARGET`. |
| 11 | [run-fixture-checks.sh:52](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/tests/run-fixture-checks.sh:52), [run-fixture-checks.sh:396](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/tests/run-fixture-checks.sh:396) | The “gitleaks detected” assertion is internally consistent because the script exits before it when gitleaks is absent, but that also skips doctor’s missing-gitleaks behavior—the exact capability branch doctor needs. The `<120s` check is machine-load dependent while three sequential lanes each permit 100 seconds. | P2 | Run doctor/SARIF/AI-config tests before the gitleaks gate; mock PATH for absent-gitleaks coverage. Use deterministic per-process bounds or a larger non-release-gating benchmark. |
| 12 | [run-fixture-checks.sh:421](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/tests/run-fixture-checks.sh:421) | SARIF fixtures never exercise a suppressed DB fingerprint with colons, suppression-reason redaction, malformed inputs, URI encoding, or logical DB locations. The shape assertions would therefore pass despite the defects above. | P2 | Add focused JSON fixtures for those cases and validate expected locations/fingerprints/absence of unsafe reason text. |
| 13 | [CHANGELOG.md:8](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/CHANGELOG.md:8), [CHANGELOG.md:18](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/CHANGELOG.md:18), [CHANGELOG.md:26](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/CHANGELOG.md:26), [plugin.json:6](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/.claude-plugin/plugin.json:6) | Claims that all invariants hold, quick-check provides the full UNKNOWN list, SARIF “adds no content”/is shareable, and the first run is one minute are not delivered by this portion. | P1 | Correct the claims or fix the preceding defects and bound the total quick-check runtime. |

Correct parts: all three Python files use only the stdlib; quick-check correctly maps gitleaks exit codes above 1 and timeouts to UNKNOWN; SS-01’s wording is honest on a successful working-tree-only scan; and SARIF has the correct version, named driver, valid level mapping, declared rules for normal results/notifications, trailing-slash base URI, and valid suppression shape for well-formed input.

The `doctor` and `quick-check` skill frontmatter is valid and their `allowed-tools` entries suffice for their documented direct Python invocation. `security-audit` has no `allowed-tools` restriction, which is valid but leaves the new SARIF command unrestricted rather than explicitly least-privileged.