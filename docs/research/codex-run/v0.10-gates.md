# Codex review gate — v0.10.0 diff (2026-09-13)

Independent adversarial review of the v0.10 diff (`git diff origin/main -- skills reference tests CHANGELOG.md`, 70 KB) by Codex CLI (`codex exec`, read-only sandbox, live web search) against SPEC.md and finding-format.md. Ten issues, no P0; all ten fixed in the same PR.

## Disposition

| # | Codex finding | Sev | Disposition |
|---|---|---|---|
| 1 | Column-level GRANT SELECT never counted; object DENY always won (the Database Engine lets a column GRANT override an object DENY) | P1 | Fixed: `effective_select()` per column implements the precedence (column DENY > column GRANT > object/schema/database DENY > CONTROL/ownership/fixed role > GRANT). Fixture: `hr.employees` object DENY with a column GRANT on `salary` |
| 2 | ALTER treated as read; DENY CONTROL ignored | P1 | Fixed: read-implying = {SELECT, CONTROL}; DENY of either at a covering scope blocks; ALTER stays write-only |
| 3 | DDM counted as protection without UNMASK / CONTROL | P1 | Fixed: `unmask_effective()` (UNMASK on column/object/schema/database, CONTROL, ownership, db_owner); masked-but-bypassed columns are DB-08 findings and DB-03 marks them "NOT binding". Fixture: role-held UNMASK on `phone` |
| 4 | SQL analytics endpoint user-identity mode not modelled | P1 | Fixed: optional `endpoint_mode.json`; user-identity mode → DB-01..DB-08 suppressed and a DB-06 says OneLake security governs; missing → DB-06; warehouse → SQL governs. Tests for all three |
| 5 | Audit coverage ignored `predicateExpression` and individual SELECT actions | P1 | Fixed: a predicate → DB-06 and no INFO coverage claim; coverage = BATCH_COMPLETED_GROUP, SCHEMA_OBJECT_ACCESS_GROUP, or any action containing SELECT |
| 6 | Schema/object ownership not captured | P1 | Fixed: `ownership.sql` (`sys.schemas.principal_id`, `sys.objects.principal_id` with schema-owner fallback); ownership is DB-01 / DB-02 / DB-ID-01 authority and satisfies UNMASK bypass. Fixture: the agent owns schema `scratch` |
| 7 | EXECUTE on procedures/functions not evaluated | P1 | Fixed: `modules.sql` + EXECUTE grants → DB-06 "opaque read path" naming the modules, and a DB-07 path |
| 8 | Unescaped `masking_function` broke the CSV | P1 | Fixed: `QUOTENAME(x, '"')` on every free-text field (doubles embedded quotes); fixture has `partial(1,"XXXXXXX",0)` and the test proves it round-trips |
| 9 | `${principal}` inside a single-quoted literal with apostrophes allowed | P2 | Fixed: validation tightened to `^[A-Za-z0-9._@ -]{1,128}$` in README, SKILL.md, and the SQL comment |
| 10 | `{}` / `{"state":"Other"}` produced DB-05 INFO | P2 | Fixed: state must be exactly Enabled/Disabled, else DB-06 and no DB-05 claim. Test |

Codex's "correct parts" (sys.database_permissions/roles as the SQL-visible source; REST `settings/sqlAudit` shape; Query Insights columns and retention; the synthetic UNION header surviving zero-row results; the ODBC-compatible sqlcmd flags) stand.

## Evidence after fixes
- `tests/test_matchers.py` PASS; `tests/run-fixture-checks.sh` PASS (Fabric step now 24 assertions); `./dev/validate.sh` PASS in docker.

## Raw output

```
| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | `scripts/dialects/fabric.py:127-133` | Column-level `GRANT SELECT` is never considered readable, and an object-level `DENY SELECT` always wins even when a column-level grant exists. Fabric supports column grants; SQL permission evaluation allows a column grant to override an object deny. This misses readable PII and can falsely report a column denied. [Microsoft Learn](https://learn.microsoft.com/en-us/sql/relational-databases/security/permissions-database-engine?view=sql-server-ver17) | P1 | Evaluate effective `SELECT` per PII column, including column grants, column denies, and the documented column-grant/object-deny exception. |
| 2 | `scripts/dialects/fabric.py:10,80-90,127-130` | `ALTER` is treated as read authority, though it does not imply `SELECT`; conversely, `DENY CONTROL` is ignored despite CONTROL covering SELECT. This produces both false DB-02/03/08 positives and false negatives. [Microsoft Learn](https://learn.microsoft.com/en-us/sql/relational-databases/security/permissions-database-engine?view=sql-server-ver16) | P1 | Replace the hand-maintained sets with a scope-aware permission hierarchy: SELECT and covering CONTROL imply read; ALTER does not; DENY of SELECT or covering CONTROL blocks it. |
| 3 | `scripts/dialects/fabric.py:147-155` | DDM is counted as protection without evaluating `UNMASK` or effective `CONTROL`. The captured grants can contain those permissions, but the evaluator ignores them, so an agent that sees raw values is described as masked. Fabric documents `UNMASK` on the column or `CONTROL` on the database as sufficient to view unmasked data. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/data-warehouse/dynamic-data-masking) | P1 | Compute effective UNMASK/CONTROL for each masked column, including inherited roles and ownership; report mask effectiveness as UNKNOWN if permission visibility is incomplete. |
| 4 | `scripts/dialects/fabric.py:65-68` | The permanent limitation omits SQL analytics endpoint access mode. In OneLake user-identity mode, table SQL grants are ignored and OneLake RLS/CLS/OLS control access; this pack can therefore emit definitive SQL-access findings for a model it does not observe. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/onelake/security/sql-analytics-endpoint-onelake-security) | P1 | Either scope the pack to Warehouse/delegated SQL endpoints, or capture endpoint access mode and OneLake policy state; make DB-01–08 UNKNOWN for table access in user-identity mode. |
| 5 | `scripts/dialects/fabric.py:176-203` | Audit coverage is inferred from two action groups only and ignores `predicateExpression`. An enabled `BATCH_COMPLETED_GROUP` can still exclude the agent or `SELECT`; a configured individual `SELECT` audit action is also wrongly reported as absent. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/data-warehouse/sql-audit-logs) | P1 | Parse and assess predicate exclusions; recognize individual `SELECT` actions as coverage. When a predicate cannot be evaluated for the agent, emit DB-06 rather than INFO coverage. |
| 6 | `sql/fabric/principals.sql:6-10`; `scripts/dialects/fabric.py:77-130` | Object and schema ownership are not captured or evaluated. An owner can bypass ordinary permission checks, so an AI principal owning a PII table/schema can be missed despite having no grant row. [Microsoft Learn](https://learn.microsoft.com/en-us/sql/relational-databases/security/permissions-database-engine?view=sql-server-ver17) | P1 | Capture `sys.objects.principal_id` and `sys.schemas.principal_id`; treat direct/effective ownership as CONTROL-equivalent or fail closed where ownership metadata is incomplete. |
| 7 | `sql/fabric/masked_views.sql:5-9`; `scripts/dialects/fabric.py:108-130` | The pack inventories only views and never evaluates `EXECUTE` on stored procedures/functions that can return PII. SQL analytics endpoints support SQL permissions for procedures and functions, so this is a material unreported read path. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/onelake/security/sql-analytics-endpoint-onelake-security) | P1 | Capture modules/procedures and `EXECUTE` grants; report PII-returning or opaque/dynamic modules as a read-path finding or DB-06 UNKNOWN. |
| 8 | `sql/fabric/pii_columns.sql:9-18` | The claimed CSV is not safely encoded. `masking_function` is concatenated unescaped; documented functions such as `random(1, 12)` and `partial(1,"XXXX",0)` contain commas, shifting fields and causing malformed or misread records. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/data-warehouse/dynamic-data-masking) | P1 | Use a robust machine-readable encoding/escaping scheme for every field, with fixtures for commas, quotes, and line breaks in identifiers and mask functions. |
| 9 | `sql/fabric/audit_logging.sql:7` | `${principal}` is inserted inside a single-quoted predicate, while the documented validation permits apostrophes. A value such as `x' OR 1=1 --` changes the count, yielding deterministic but false audit evidence. | P2 | Escape single quotes before substitution, or pass a safely encoded sqlcmd variable/parameter; add an adversarial-principal fixture. |
| 10 | `scripts/dialects/fabric.py:176-200` | A syntactically valid but incomplete `audit_status.json` (for example `{}` or `{"state":"Other"}`) sets `state` to `""`, bypasses DB-06, and emits DB-05 INFO. This violates the fail-closed finding contract. | P2 | Require `state` to be exactly Enabled/Disabled and validate optional field types; otherwise emit DB-06 and no affirmative DB-05 status. |

Correct parts:

- `sys.database_permissions`/roles are an appropriate SQL-visible source, and Microsoft confirms they do not reveal workspace-role or item-permission access. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/data-warehouse/sql-granular-permissions)
- The REST endpoint and its `state`, `retentionDays`, and `auditActionsAndGroups` shape are correct for a Warehouse. [Microsoft Learn](https://learn.microsoft.com/en-gb/fabric/data-warehouse/configure-sql-audit-logs?tabs=browser)
- Query Insights supports `queryinsights.exec_requests_history`, `login_name`, and 30-day history. [Microsoft Learn](https://learn.microsoft.com/en-us/fabric/data-warehouse/query-insights)
- The sqlcmd formatting flags used are valid ODBC-compatible flags; the synthetic UNION header remains present even when the catalog query contributes zero rows. [Microsoft Learn](https://learn.microsoft.com/en-us/sql/tools/sqlcmd/sqlcmd-utility?view=azure-sqldw-latest)```
