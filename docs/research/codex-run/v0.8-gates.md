# Codex review gate — v0.8.0 diff (2026-09-13)

Independent adversarial review of the v0.8 diff (`git diff origin/main -- skills reference tests CHANGELOG.md`, 65 KB) by Codex CLI (`codex exec`, read-only sandbox, live web search) against SPEC.md and finding-format.md. Nine issues, no P0; all nine fixed in the same PR, each verified against the AWS page Codex cited (fetched live).

## Disposition

| # | Codex finding | Sev | Disposition |
|---|---|---|---|
| 1 | DB-08 matched `input_columns`; a policy protects its `output_columns`, and the view records a JSON-like array | P1 | Fixed: pack captures both; the module parses `["a","b"]` (brace form tolerated) and matches output columns only. Fixture adds a PUBLIC mask on `full_name` (covered) beside the hr-only email mask (not covered); unit tests ×6 |
| 2 | `auditor_sees_grants` treated `sys:monitor` etc. as sufficient and was never used; a non-superuser capture could yield clean DB-01/02/03/07 | P1 | Fixed: `audit_logging.csv` records `auditor_is_superuser`; below superuser every grant-derived verdict carries a DB-06 "partial", and an empty grant set produces an explicit "not a clean result" DB-06. Two fixture tests |
| 3 | "UNLOAD/COPY reach is SQL-invisible" was false: `SVV_IAM_PRIVILEGES` exists | P1 | Fixed: `iam_privileges.sql` + evaluation through the identity closure; UNLOAD / EXTERNAL FUNCTION / CREATE MODEL → DB-10 CRITICAL, COPY → HIGH; the DB-06 narrowed to bucket-level IAM/S3 authorization |
| 4 | Ownership is not a grant row; an owner of a sensitive table passed DB-01 | P1 | Fixed: `ownership.sql` (`pg_class` → `pg_user`); ownership by the principal, its roles, or groups is DB-01 write authority and DB-ID-01 evidence; owned relations count as readable |
| 5 | Column-level SELECT/UPDATE (`SVV_COLUMN_PRIVILEGES`) absent; `SELECT(ssn)` alone passed DB-03/DB-08 | P1 | Fixed: `column_grants.sql`; column SELECT feeds readability, DB-03, DB-08, DB-07; column UPDATE feeds DB-01. Fixture: `hr.employees(ssn, salary)` |
| 6 | Unconditional casefolding unsound under `enable_case_sensitive_identifier=true`; quoted usernames keep case | P1 | Fixed: the setting is captured; object names lowercase only when it is false, identity names compare as recorded. Fixture test: with the setting on, a PUBLIC mask on `full_name` does not cover `Full_Name` |
| 7 | Database-scoped `TABLES` grants (`SVV_DATABASE_PRIVILEGES`) not modelled | P1 | Fixed: `database_grants.sql`; database-scoped TABLES read/write → broad DB-02 / DB-01 and DB-07; SCHEMAS-scope USAGE makes every schema usable. Fixture test |
| 8 | DB-04 asserted "no governed layer" while admitting the policy inventory was invisible | P2 | Fixed: below `sys:secadmin` with no masking-signal view, DB-04 is a DB-06 UNKNOWN |
| 9 | DB-10 counted any external-schema privilege row as reach | P2 | Fixed: only USAGE counts; the SQL comment says so |

Codex's "correct parts" (`ANY(g.grolist)`; role-edge direction; `LOWER()` with the case-sensitive `~`; `:'ai_user'`; `current_setting`; `IAM:`/`IAMA:`; zero-row masking views; `eskind` mapping) stand.

## Evidence after fixes
- `tests/test_matchers.py` PASS; `tests/run-fixture-checks.sh` PASS (Redshift step now 24 assertions incl. partial-capture, empty-grants-not-clean, case-sensitive mask, database-scoped grant, DB-04 UNKNOWN); `./dev/validate.sh` PASS in docker.

## Raw output

```
No P0 findings. Line numbers below are new-file line numbers.

| # | file:line | issue | severity P0/P1/P2 | fix |
|---|---|---|---|---|
| 1 | `sql/redshift/policy_attachment.sql:6`; `scripts/dialects/redshift.py:163-168` | DB-08 treats `input_columns` as the protected columns. Redshift applies a DDM policy to `output_columns`; `input_columns` are optional values used by the expression and can differ. AWS’s documented output is JSON-like (`["col"]`), which the current brace-only parser also fails to normalize. A policy masking `ssn` using `country` as input is reported as not covering `ssn`. | P1 | Capture `output_columns` (and optionally `input_columns` separately), parse the documented array/path form, and match output column/path identifiers only. [AWS DDM attachment view](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_ATTACHED_MASKING_POLICY.html), [ATTACH MASKING POLICY](https://docs.aws.amazon.com/redshift/latest/dg/r_ATTACH_MASKING_POLICY.html) |
| 2 | `scripts/dialects/redshift.py:69-70,99-141,236-249` | Grant-dependent results can fail open under a partial capture. `SVV_RELATION_PRIVILEGES` is complete only for superusers or users with `SYSLOG ACCESS UNRESTRICTED`; `sys:monitor` provides catalog/system-table access, not that permission. The code computes `auditor_sees_grants`, incorrectly treats several system roles as sufficient, then never uses it to emit an UNKNOWN. A non-superuser capture can therefore produce clean DB-01/02/03/07 results while grants to an indirect identity were omitted. | P1 | Record and verify the actual relation-grant visibility capability; unless it is superuser or explicit SYSLOG unrestricted, emit DB-06 UNKNOWN and prevent grant-derived passes. [SVV_RELATION_PRIVILEGES visibility](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_RELATION_PRIVILEGES.html), [system roles](https://docs.aws.amazon.com/redshift/latest/dg/r_roles-default.html), [SYSLOG access](https://docs.aws.amazon.com/redshift/latest/dg/r_ALTER_USER.html) |
| 3 | `sql/redshift/external_paths.sql:5-13`; `scripts/dialects/redshift.py:251-263` | The claimed permanent SQL-invisible UNLOAD/COPY reach is false. `SVV_IAM_PRIVILEGES` exposes explicit COPY and UNLOAD IAM permissions, including user, role, group, and PUBLIC grantees. The pack neither captures nor evaluates it, so it misses a directly granted UNLOAD path and labels the auditable portion UNKNOWN. | P1 | Add an `iam_privileges.sql` capture over `SVV_IAM_PRIVILEGES`; resolve it through the same identity closure and report COPY/UNLOAD grants. Keep a narrow UNKNOWN only for IAM-policy/S3-resource authorization that the view cannot establish. [SVV_IAM_PRIVILEGES](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_IAM_PRIVILEGES.html) |
| 4 | `sql/redshift/grants.sql:5-9`; `scripts/dialects/redshift.py:99-114` | Ownership is not a grant row. The pack only reads explicitly granted relation privileges, yet an AI user who owns a relation can modify or destroy it even with no explicit privilege. DB-01 can therefore return clean for an owner of a sensitive table. | P1 | Capture object/schema ownership (for local objects) and treat ownership as effective write authority; report datashare contexts separately where local catalogs are unavailable. [SVV_RELATION_PRIVILEGES is explicit grants only](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_RELATION_PRIVILEGES.html), [owner permissions](https://docs.aws.amazon.com/redshift/latest/dg/r_Privileges.html) |
| 5 | `sql/redshift/grants.sql:5-9`; `scripts/dialects/redshift.py:115-141` | Column-level SELECT/UPDATE grants are absent from `SVV_RELATION_PRIVILEGES`. An agent granted only `SELECT(ssn)` has no relation-level row, so DB-03 and DB-08 see the table as unreadable and can return clean despite direct PII access. | P1 | Capture `SVV_COLUMN_PRIVILEGES`; incorporate column-level SELECT/UPDATE into readability, PII exposure, masking coverage, and write findings. [SVV_COLUMN_PRIVILEGES](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_COLUMN_PRIVILEGES.html), [SHOW COLUMN GRANTS](https://docs.aws.amazon.com/redshift/latest/dg/r_SHOW_COLUMN_GRANTS.html) |
| 6 | `scripts/dialects/redshift.py:34-35,71,89,130-134,166-168` | Unconditional Unicode casefolding is unsound. With `enable_case_sensitive_identifier=true`, Redshift can have both `"c"` and `"C"`; a mask on one will be treated as covering both. Quoted usernames preserve case regardless of that setting. This can hide an unmasked PII column or merge different principals. | P1 | Capture `current_setting('enable_case_sensitive_identifier')`; compare exact identifiers when enabled, and preserve username case in all modes. Add a fixture with `"c"`/`"C"` and a mask on only one. [case-sensitive identifiers](https://docs.aws.amazon.com/redshift/latest/dg/r_enable_case_sensitive_identifier.html) |
| 7 | `sql/redshift/schema_grants.sql:3-5`; `scripts/dialects/redshift.py:118-131` | The evaluator models schema-scoped `TABLES` grants but omits database-scoped `TABLES` privileges. Redshift can grant SELECT/INSERT/UPDATE/etc. for all tables in a database, including the documented `privilege_scope = TABLES`; those can yield no captured schema-grant row. | P1 | Capture and resolve `SVV_DATABASE_PRIVILEGES` (or a bounded `SHOW GRANTS FOR` representation) alongside schema grants, including `TABLES` scope. [SVV_DATABASE_PRIVILEGES](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_DATABASE_PRIVILEGES.html), [database-scoped grant output](https://docs.aws.amazon.com/redshift/latest/dg/r_SHOW_GRANTS.html) |
| 8 | `scripts/dialects/redshift.py:151-188` | The policy fail-closed rule is only applied to DB-08. With an empty policy file from a non-secadmin auditor and no masking-signal view, DB-04 emits a definitive “No masked/governed view layer” finding, even while its own evidence admits the policy inventory is not visible. That is neither a verified finding nor an UNKNOWN under the contract. | P2 | When policy visibility is incomplete, report DB-04 as UNKNOWN or limit it strictly to the independently observable view-heuristic result; do not assert absence of governed controls. [SVV_ATTACHED_MASKING_POLICY visibility](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_ATTACHED_MASKING_POLICY.html) |
| 9 | `scripts/dialects/redshift.py:252-260` | DB-10 calls any schema-privilege row an “External schema usable” path. Actual external-schema access requires `USAGE`; the join also returns non-USAGE privileges, so a non-access grant can produce a HIGH reachability false positive. | P2 | Filter DB-10 reachability to effective `USAGE` grants and retain other privileges only as supplemental evidence. [CREATE EXTERNAL SCHEMA permissions](https://docs.aws.amazon.com/redshift/latest/dg/r_CREATE_EXTERNAL_SCHEMA.html) |

Correct parts:

- `ANY(g.grolist)` is valid Redshift SQL; AWS documents that exact group-membership join. [AWS Groups](https://docs.aws.amazon.com/redshift/latest/dg/r_Groups.html)
- The role-edge direction is correct: `role_name` is the role receiving `granted_role_name`, so the transitive walk follows the effective inheritance direction. [SVV_ROLE_GRANTS](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_ROLE_GRANTS.html)
- `~` is case-sensitive in Redshift, so applying `LOWER()` to PII-name matching is correct for the default identifier mode. [POSIX operators](https://docs.aws.amazon.com/redshift/latest/dg/pattern-matching-conditions-posix.html)
- `:'ai_user'` is the safe psql literal-substitution form. [psql interpolation](https://www.postgresql.org/docs/current/app-psql.html)
- `current_setting('enable_user_activity_logging')`, the IAM/IAMA temporary-credential prefixes, masking-view zero-row behavior for regular users, and the mapped external-schema `eskind` values are all supported by the vendor documentation. [CURRENT_SETTING](https://docs.aws.amazon.com/redshift/latest/dg/r_CURRENT_SETTING.html), [GetClusterCredentials](https://docs.aws.amazon.com/cli/latest/reference/redshift/get-cluster-credentials.html), [external schemas](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_EXTERNAL_SCHEMAS.html)```
