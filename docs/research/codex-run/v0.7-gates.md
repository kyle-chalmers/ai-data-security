# Codex review gate — v0.7.0 diff (2026-09-13)

Independent adversarial review of the v0.7 diff (`git diff origin/main -- skills reference tests CHANGELOG.md`, 74 KB) by Codex CLI (`codex exec`, read-only sandbox, live web search) against SPEC.md and finding-format.md. Nine issues, no P0; all nine fixed in the same PR.

## Disposition

| # | Codex finding | Sev | Disposition |
|---|---|---|---|
| 1 | Documented capture `sed … \| dbsqlcli -e "$(cat)"` is broken: `$(cat)` expands before the pipe | P1 | Fixed: SQL is passed as text, `dbsqlcli --table-format csv -e "$(sed … file.sql)"`, in README, SKILL.md, and the module's `CAPTURE` string (which every DB-06 quotes) |
| 2 | `SHOW GROUPS WITH USER` always; a group principal needs `WITH GROUP` | P1 | Fixed: `${principal_kind}` placeholder (USER for users and service principals, GROUP for groups), derived by the skill from the principal's shape; SCIM fallback documented for applicationIds a workspace rejects |
| 3 | Schema/catalog ownership discarded; only table owners evaluated | P1 | Fixed: `grants.sql` joins `schemata.schema_owner` and `catalogs.catalog_owner`; ownership at any level (by the principal or its groups) is DB-01 evidence and DB-ID-01 evidence |
| 4 | `MANAGE` ignored (grant-yourself-anything control plane) | P1 | Fixed: MANAGE at any level → DB-ID-01 CRITICAL ("can grant itself any privilege below") and a DB-07 path; kept distinct from DML |
| 5 | DB-10 missed catalog/schema-level READ VOLUME / WRITE VOLUME | P1 | Fixed: those rows cascade into DB-10 with "cascades to every volume below" evidence |
| 6 | "Readable" meant SELECT alone; Unity Catalog needs USE CATALOG + USE SCHEMA | P2 | Fixed: effective access requires the USE path (or ownership / ALL PRIVILEGES); SELECT without it is a LOW "latent grant" DB-02 and is excluded from DB-03/DB-08 exposure. Fixture: the service principal's SELECT on `app.orders` is latent |
| 7 | Case-sensitive grantee matching vs lowercase INFORMATION_SCHEMA | P2 | Fixed: all identifier comparisons casefolded, recorded spelling kept for evidence; test with a mixed-case `--role` |
| 8 | `VIEW_DEFINITION` is NULL for non-owners; `NULL RLIKE` read as "no masking" | P2 | Fixed: `masked_views.sql` exports `definition_visible`; invisible definitions make DB-04 a DB-06 UNKNOWN naming the views |
| 9 | Snowflake `CURRENT_SECONDARY_ROLES()` not parsed; `ALL` substring heuristic | P1 | Fixed: `_secondary_roles()` parses the documented JSON (`roles` comma-separated string; list accepted), names active roles in DB-ID-01, and adds a DB-06 that the session's effective access is wider than every verdict; unparseable → DB-06. Fixture uses the documented shape; unit tests ×7 |

Codex's "correct parts" (privilege views and `INHERITED_FROM`; table-type set; `COLUMN_MASKS`/`ROW_FILTERS`; `RLIKE '(?i)…'`; zero-row header validation; permanent visibility DB-06; planner refusal) stand.

## Evidence after fixes
- `tests/test_matchers.py` PASS; `tests/run-fixture-checks.sh` PASS (Databricks step now 17 assertions incl. casefold, latent grants, MANAGE, ownership, cascading volumes, invisible view definitions); `./dev/validate.sh` PASS in docker.

## Raw output

```
No P0 findings. I found 9 concrete issues.

| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | skills/db-access-audit/SKILL.md:79 | The advertised capture pipeline is broken: `$(cat)` is expanded before the pipeline runs and reads the shell’s stdin, not `sed` output. `dbsqlcli -e` therefore receives an empty/unrelated query, so live captures cannot produce the required CSVs. Databricks supports passing either SQL text or a file to `-e`. [Docs](https://docs.databricks.com/aws/en/dev-tools/databricks-sql-cli) | P1 | Use `dbsqlcli --table-format csv -e "$(sed ... file.sql)"`, or write the substituted SQL to a restricted temporary `.sql` file and pass that path to `-e`. Update the identical `CAPTURE` string and README. |
| 2 | skills/db-access-audit/sql/databricks/identity.sql:7 | The pack always uses `SHOW GROUPS WITH USER`, although a group principal requires `WITH GROUP`; the adjacent comment even says so. A group configured as `--role` therefore fails identity capture and cannot have its group-derived grants evaluated. [SHOW GROUPS syntax](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-syntax-aux-show-groups) | P1 | Choose `WITH GROUP` when the validated principal is a group; define/document handling for service principals separately. |
| 3 | skills/db-access-audit/sql/databricks/grants.sql:13 | Schema/catalog ownership is discarded (`owner` is `NULL`), and the evaluator only tests table ownership. A principal owning a schema or catalog can manage privileges on child objects, but this can yield no DB-01/DB-ID-01 finding. [SCHEMATA](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/schemata), [CATALOGS](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/catalogs), [ownership effects](https://docs.databricks.com/aws/en/data-governance/unity-catalog/manage-privileges) | P1 | Join/select `schema_owner` and `catalog_owner`, preserve them in the recorded schema, and report parent ownership as a privilege-escalation path. |
| 4 | skills/db-access-audit/scripts/dialects/databricks.py:12 | `MANAGE` on catalog/schema/table/view is ignored: it is neither in `WRITE_PRIVS` nor independently checked. It lets the holder manage grants (and, at catalog scope, manage/transfer/drop the catalog), so a clean DB-01 is possible for a principal that can grant itself data access. [Privilege requirements](https://docs.databricks.com/aws/en/data-governance/unity-catalog/access-control/permissions-concepts) | P1 | Evaluate `MANAGE` as a separate high/critical control-plane finding, including inherited/group paths; do not imply it is equivalent to direct DML. |
| 5 | skills/db-access-audit/scripts/dialects/databricks.py:206 | DB-10 only examines `external_paths.csv`. It misses `READ VOLUME`/`WRITE VOLUME` granted at catalog or schema level, even though those privileges cascade to contained volumes. [Volume privilege inheritance](https://docs.databricks.com/aws/en/volumes/privileges) | P1 | Include catalog/schema `READ VOLUME` and `WRITE VOLUME` rows from `grants.csv` in DB-10, with effective usage checks and inherited-path evidence. |
| 6 | skills/db-access-audit/scripts/dialects/databricks.py:87 | “Readable” means `SELECT` alone. Unity Catalog requires `SELECT` plus `USE CATALOG` and `USE SCHEMA`; a standalone/incomplete SELECT grant is reported as actual DB-02/03/08 exposure. [Unity Catalog privilege requirements](https://docs.databricks.com/aws/en/data-governance/unity-catalog/access-control/privileges-reference) | P2 | Compute effective access only when all required usage privileges are present through direct, group, or inherited grants; otherwise report the incomplete grant separately or as non-exploitable. |
| 7 | skills/db-access-audit/scripts/dialects/databricks.py:50 | Grantee matching is case-sensitive, although Databricks stores INFORMATION_SCHEMA identifiers in lowercase. A valid mixed-case `--role` can yield `mine = []` and a clean recorded result. [Information schema behavior](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-information-schema) | P2 | Normalize the supplied role, grantees, group names, owners, and special groups with `casefold()` before matching; retain original display values for evidence. |
| 8 | skills/db-access-audit/sql/databricks/masked_views.sql:4 | `VIEW_DEFINITION` is `NULL` unless the capture user owns the view. `NULL RLIKE ...` is not a masking signal, so the evaluator can emit DB-04 “no masked layer” despite unreadable definitions rather than fail closed. [VIEWS definition visibility](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/views) | P2 | Export a definition-visibility state; if any relevant definition is unavailable, make the view-layer determination UNKNOWN rather than treating it as unmasked. |
| 9 | skills/db-access-audit/scripts/eval_grants.py:486 | Snowflake secondary-role detection does not parse the JSON result. It only flags strings containing `ALL`, missing an explicitly activated role such as `{"roles":"POWER_ROLE","value":"POWER_ROLE"}`; it can also false-positive on a role name containing `ALL`. Snowflake documents `roles` and `value` as JSON object fields, with `roles` represented as a comma-separated string. [CURRENT_SECONDARY_ROLES](https://docs.snowflake.com/en/sql-reference/functions/current_secondary_roles) | P1 | `json.loads` the value, normalize the documented string/list defensively, and report any nonempty active `roles` set. Re-run privilege evaluation for those roles or explicitly make affected access checks UNKNOWN. |

Correct parts:

- The selected Databricks INFORMATION_SCHEMA privilege views and their `INHERITED_FROM` columns are valid, and the table-type set includes managed, external, foreign, streaming, materialized-view, and shallow-clone values. [TABLE_PRIVILEGES](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/table_privileges), [TABLES](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/tables)
- `COLUMN_MASKS`, `ROW_FILTERS`, `RLIKE '(?i)...'`, and `||` use valid Databricks SQL constructs; the pack outputs names/booleans rather than sampled data or view text.
- The zero-row CSV header validation change is correctly fail-closed for missing required headers.
- The permanent INFORMATION_SCHEMA visibility DB-06 warning is conservative and appropriate.
- The planner refusal for Databricks is explicit and does not render unreviewed DDL.```
