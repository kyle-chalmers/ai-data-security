# Codex review gate for v0.4.0 (decision-quality DB audit)

Per the standing directive, Codex (`codex exec -s read-only`) reviewed the full v0.4 diff before it
was committed. Raw review follows the disposition table.

| # | Severity | Disposition | What changed |
|---|---|---|---|
| 1 | P1 | **Accepted** (verified against the SHOW GRANTS syntax page: no `OF DATABASE ROLE` form) | `audit_quality.sql` statement 1 now reads `SNOWFLAKE.ACCOUNT_USAGE.GRANTS_TO_ROLES` filtered to `GOVERNANCE_VIEWER`; precondition text adds `SECURITY_VIEWER`; fixture re-recorded to the SELECT shape |
| 2 | P1 | **Accepted** | PUBLIC grants read from `pg_class.relacl` via `aclexplode` with grantee OID 0; golden regenerated in docker (same rows, now from a source a least-privileged auditor can see) |
| 3 | P1 | **Accepted** | `REQUIRED_COLUMNS_SF` per recorded statement; `parse_show_tables` keeps headers for empty tables; any renamed/missing column fails closed to DB-06 and skips dependent checks. Fixture: renamed `granted_on` → no DB-01/02/07/10 |
| 4 | P1 | **Accepted** | `DESCRIBE USER` without `TYPE` or `DEFAULT_SECONDARY_ROLES` is DB-06, never DB-ID-01. Fixture added |
| 5 | P1 | **Accepted** | `execute_as_user` empty / `NULL` / `NONE` all count as unset; the recorded fixture now carries a literal `NULL`; `SHOW DYNAMIC TABLES` column verified against the Snowflake docs |
| 6 | P1 | **Accepted** | Postgres DB-07 requires a parsed `pii_columns` input and a real object match; a missing PII input yields a DB-06 naming DB-07. Fixture added |
| 7 | P1 | **Accepted** | `external_paths.sql` now captures EXECUTE on `dblink*` / `pg_read_file` / `pg_ls_dir` / `lo_import` / `lo_export`; DB-10 evaluates those and foreign servers; installed extensions are context text only; CHANGELOG reworded |
| 8 | P1 | **Accepted** | DB-ID-01 flags a `DEFAULT_ROLE` different from the audited role. Fixture added. Capturing `CURRENT_ROLE()` of the audit session is noted for v0.5 |
| 9 | P2 | **Accepted** | Postgres "without inputs" asserts absence of all five IDs; Snowflake asserts absence of DB-ID-01, DB-09 and any non-INFO DB-08 (DB-07/DB-10 derive from the base `grants` input and are legitimately present) |
| 10 | P2 | **Accepted** | CHANGELOG narrows the claim to DB-01..DB-05 finding payloads and names the two visible differences |

Also confirmed correct by the reviewer: read-only intent of every new statement; PG16 compatibility of `inherit_option`, `aclexplode`, `regnamespace`; NOINHERIT edges stop inheritance; every new finding passes through the confidence cap; verdicts stay in the script; the precondition is shown in the gate.

### Raw review

| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | [audit_quality.sql:6](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/sql/snowflake/audit_quality.sql:6) | `SHOW GRANTS OF DATABASE ROLE ...` is not valid Snowflake syntax; `SHOW GRANTS OF` supports account roles, application roles, and service roles—not database roles. DB-09 cannot run live, while the fixture encodes an impossible result. | P1 | Replace with a documented, privilege-appropriate query (likely `ACCOUNT_USAGE.GRANTS_TO_ROLES` under the stated `SECURITY_VIEWER` precondition), and update the gate/docs/fixture. [Snowflake SHOW GRANTS syntax](https://docs.snowflake.com/en/sql-reference/sql/show-grants) |
| 2 | [identity.sql:41](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/sql/postgres/identity.sql:41) | DB-07’s PostgreSQL `PUBLIC` branch is impossible: `information_schema.role_table_grants` explicitly omits privileges granted through `PUBLIC`. The committed [golden:14](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/tests/fixtures/postgres/expected/identity.csv:14) therefore encodes an output this query should not produce on PG16. | P1 | Read ACLs from `pg_class.relacl`/`aclexplode` and explicitly handle grantee OID 0 (`PUBLIC`); avoid information-schema visibility filtering. [PostgreSQL role_table_grants](https://www.postgresql.org/docs/16/infoschema-role-table-grants.html) |
| 3 | [eval_grants.py:214](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/scripts/eval_grants.py:214) | Snowflake recorded inputs are accepted solely by table count, with no required-header validation. A `grants.txt` lacking `granted_on` silently misses DB-01/DB-07/DB-10; an unrelated empty table can be treated as zero policy references and produce DB-08. This violates fail-closed handling for malformed captures. | P1 | Define and validate required columns per Snowflake statement before any verdict; emit DB-06 and skip dependent checks on absent/renamed columns. |
| 4 | [eval_grants.py:377](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/scripts/eval_grants.py:377) | A `DESCRIBE USER` capture without a `TYPE` property becomes `TYPE = unknown` and emits DB-ID-01 HIGH instead of DB-06 UNKNOWN. This is a claimed v0.4 finding without its required evidence. | P1 | Require `TYPE`, `DEFAULT_SECONDARY_ROLES`, and a valid grants-table shape; missing required identity evidence must yield DB-06. |
| 5 | [eval_grants.py:455](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/scripts/eval_grants.py:455) | Snowflake documents default `execute_as_user` as `NULL`; the evaluator only flags an empty string. A normal `snow sql` rendering of `NULL` is therefore a DB-08 dynamic-table false negative. The fixture’s blank cell hides it. | P1 | Treat empty, `NULL`, and `NONE` as unset; add a recorded fixture with literal `NULL`. The field itself is real, including in current `SHOW DYNAMIC TABLES` output. [Snowflake output definition](https://docs.snowflake.com/en/sql-reference/sql/show-dynamic-tables) |
| 6 | [eval_grants.py:556](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/scripts/eval_grants.py:556) | If `pii_columns` is missing or legitimately empty, `or not pii_tables` flags every inherited/PUBLIC `SELECT` as a HIGH finding while claiming it reaches “PII-bearing objects.” Thus a missing base input can create DB-07 rather than only an UNKNOWN. | P1 | Require a successfully parsed PII input and an actual object match for the PII-specific DB-07 claim; otherwise emit/retain DB-06 only. |
| 7 | [eval_grants.py:627](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/scripts/eval_grants.py:627) | `external_paths.sql` captures `dblink`/`file_fdw`/`postgres_fdw` extensions, but DB-10 ignores every `kind = extension` row. The changelog’s “network extensions” detection claim is not delivered. | P1 | Either evaluate these rows with appropriately scoped function/server privilege evidence, or remove the claim and pack output. |
| 8 | [eval_grants.py:382](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/skills/db-access-audit/scripts/eval_grants.py:382) | DB-ID-01 never compares the user’s `DEFAULT_ROLE` (nor captured session role) to `--role`. A service user whose actual/default role is a broad, single role can be audited as a different narrow role with no identity finding. | P1 | Capture `CURRENT_ROLE()` for the audit session or at minimum compare `DEFAULT_ROLE` to `--role`; mismatch must be DB-06/DB-ID-01, not clear. |
| 9 | [tests/run-fixture-checks.sh:334](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/tests/run-fixture-checks.sh:334) | The advertised “no v0.4 finding without inputs” assertion omits DB-07. Combined with the invalid Snowflake DB-09 fixture and blank dynamic-table `NULL`, the tests prove fixture expectations, not live behavior. | P2 | Assert absence of all five new IDs, add malformed-header/missing-`TYPE`/literal-`NULL` cases, and use syntactically valid Snowflake recordings. |
| 10 | [CHANGELOG.md:11](/Users/kchalmers/Development/ai-data-security/.claude/worktrees/ai-security-plugin-research-ae359d/CHANGELOG.md:11) | “Existing checks byte-identical on the same inputs” is not literally true for evaluator output: tool version changes and omitted v0.4 captures add four DB-06 entries. | P2 | Narrow the statement to existing DB-01–DB-05 finding payloads, or add a regression test proving that narrower invariant. |

Correct parts:

- The newly added SQL statements are syntactically read-only in intent: PostgreSQL uses catalog `SELECT`/CTEs and Snowflake uses `SHOW`/`SELECT`; none selects application-table row data.
- PostgreSQL 16 compatibility is sound for `pg_auth_members.inherit_option`, `aclexplode`, and the `regnamespace` cast.
- PostgreSQL’s inheritance traversal correctly stops automatic inherited grants across `NOINHERIT` membership edges. [PostgreSQL role-membership behavior](https://www.postgresql.org/docs/16/role-membership.html)
- All new emitted findings go through `finding()`, so the confidence severity cap is respected.
- The Snowflake DB-08 matcher accepts the documented canonical `MASKING_POLICY` spelling (and harmlessly accepts a space variant).
- Verdict computation is in the script, not delegated to the model.
- The `GOVERNANCE_VIEWER` precondition is stated before execution in the visible skill gate, though DB-09’s current SQL does not implement it correctly.