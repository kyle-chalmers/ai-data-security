# Codex review gate — v0.6.0 diff (2026-09-12)

Independent adversarial review of the v0.6 diff (`git diff origin/main -- skills reference tests scripts dev/validate.sh CHANGELOG.md SPEC.md`, 131 KB) by Codex CLI (`codex exec`, read-only sandbox, live web search) against SPEC.md, finding-format.md and the planner amendment. Twelve issues; eleven fixed in the same PR, one accepted as a documented limitation with a mitigation.

## Disposition

| # | Codex finding | Sev | Disposition |
|---|---|---|---|
| 1 | `--vault-schema app` (a raw schema) accepted; rollback would `DROP SCHEMA app CASCADE` | **P0** | Fixed: curated/vault must not equal any raw schema the audit saw (case-insensitive) nor a reserved schema; exit 2, nothing rendered. Fixture test |
| 2 | `--owner-role` may equal the AI role, granting the agent raw reads as the view owner | P1 | Fixed: owner ≠ AI role; Snowflake auditor ≠ AI role and ≠ owner. Fixture tests |
| 3 | `finding.check_id` echoed unvalidated into the header comment; a newline escapes the comment | P1 | Fixed: only `^[A-Z]{2,4}(-[A-Z0-9]+)+$` check ids are printed; every other audit-JSON string was already validated or hashed. Fixture test with a hostile check_id |
| 4 | Pseudonymized tier by name only: a base-table `ssn_hash` bypasses DB-03/DB-08 (fail-open) | P1 | Fixed: the pack no longer tiers by suffix. The evaluator tiers `Pseudonymized` only when the suffix matches AND the object is a view whose definition shows a hashing/masking signal (Postgres `has_masking_signal`; Snowflake `SHOW VIEWS` `text`); no views input → nothing pseudonymized. Unit tests + fixture test (`ssn_hash` on a base table stays Restricted HIGH) |
| 5 | Snowflake suffix regex unanchored (`ssn_hash_backup`) | P1 | Moot after #4: one anchored Python regex (`$`) for both dialects; unit-tested |
| 6 | `ALTER DEFAULT PRIVILEGES` without `FOR ROLE` edits the DBA's own defaults, not the creator's; global defaults ignored | P1 | Fixed: `identity.sql` records the grantor (`default_acl` detail = `grantor:PRIVILEGE`, golden re-recorded), `plan_inputs.default_acl` is structured, templates emit `FOR ROLE <grantor> [IN SCHEMA …]` and a global variant; a missing grantor makes the plan INCOMPLETE. Rollback mirrors it. CI re-audit proves DB-07's default-privilege path is gone |
| 7 | One STRING masking policy attached to every Restricted column; DATE/NUMBER columns fail | P1 | Fixed: `plan_inputs.pii_columns[].type` captured; the policy attaches only to STRING-family columns, others are listed as INCOMPLETE. Fixture test with a DATE column |
| 8 | Curated view names collide across schemas (`sales.customers` vs `support.customers`) | P1 | Fixed: `view_names()` uses `<schema>_<table>` whenever a basename repeats; deterministic. Unit + fixture test |
| 9 | PUBLIC revocations auto-emitted with `ALL`, rollback restored only SELECT | P1 | Fixed: rendered commented out with the exact captured privilege unless `--include-public-revokes` (operator-confirmed); rollback re-grants the exact privilege. SKILL.md tells the model to ask first. The CI harness passes the flag as the confirming human |
| 10 | `DROP OWNED BY owner_role` in rollback is unsafe for a pre-existing role | P1 | Fixed: rollback drops the two plan schemas CASCADE, revokes exactly the owner's raw-schema reads, then `DROP ROLE IF EXISTS`; section 2's plain `CREATE ROLE` fails if the role pre-exists, on purpose and now documented |
| 11 | Table whose every column is Restricted renders `SELECT` + comments + `FROM` (invalid SQL) | P2 | Fixed: empty projection → INCOMPLETE placeholder, never executable text |
| 12 | Snowflake validation is comments plus intentionally failing statements; no harness proves it | P2 | Partially fixed / accepted: section 6 is now a set of statements that each SUCCEED and return `expected` vs `actual` (`INFORMATION_SCHEMA.TABLE_PRIVILEGES`, `CURRENT_SECONDARY_ROLES()`, curated counts); the erroring probes are commented out as optional. A live Snowflake integration harness needs an account the project does not have; the header and README say Snowflake support is fixture-validated |

Codex's "correct parts" (SECURITY DEFINER + explicit search_path; owner-privilege views; SERVICE_AGENT / IS_AGENT_ACTIVATED() / CURRENT_SECONDARY_ROLES() / scalar SQL-UDF query bodies / REVOKE ON FUTURE TABLES; the pseudonymization statement) stand.

## Evidence after fixes
- `tests/test_matchers.py` PASS (adds `pseudonymized_set` ×4, `view_names`, `CHECK_ID`).
- `tests/run-fixture-checks.sh` PASS (14 new planner assertions incl. every refusal above).
- `tests/postgres-fixture-check.sh` PASS in docker: apply → 19/19 validation rows ok as `ai_agent` → raw read refused → re-audit clean of DB-01/02/03/04/07/08/10/ID-01 → rollback → pack outputs identical to goldens.
- `./dev/validate.sh` PASS.

## Raw output

```
| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | skills/safe-db-access/scripts/plan.py:331 | Valid identifiers can collide with source schemas. `--vault-schema app` is accepted when `app` is a raw schema; rollback then executes `DROP SCHEMA app CASCADE`. This can destroy production objects. | P0 | Reject curated/vault schemas that equal any captured raw schema; require fresh, operator-confirmed namespaces and never emit destructive rollback for pre-existing namespaces. |
| 2 | skills/safe-db-access/scripts/plan.py:319 | `--owner-role` may equal `--ai-role`. In Postgres, the plan then grants the AI role raw-schema `USAGE` and `SELECT` as the view owner, defeating the intended boundary. | P1 | Require owner, AI role, and (Snowflake) auditor role to be distinct; reject unsafe relationships before rendering. |
| 3 | skills/safe-db-access/scripts/plan.py:344 | `finding.check_id` is copied unvalidated into the SQL header. A crafted audit JSON can use a newline plus SQL to escape the `-- Audit findings` comment, so rendered output is no longer fixed-template SQL. | P1 | Accept only known check IDs or a strict identifier pattern; otherwise omit/hash them. Treat every audit-JSON string as untrusted output. |
| 4 | skills/db-access-audit/sql/postgres/pii_columns.sql:15 | A raw column named `ssn_hash` is downgraded to Pseudonymized, omitted from DB-08, and passed through unchanged by the planner. Naming is explicitly not evidence of keyed hashing, so this is a fail-open raw-PII bypass. | P1 | Keep name-only candidates as a distinct warning, but retain Restricted/Confidential exposure checks until a deterministic provenance/metadata signal verifies the transformation. |
| 5 | skills/db-access-audit/sql/snowflake/pii_columns.sql:15 | The Snowflake pseudonymization regex lacks `$`; `ssn_hash_backup` is classified Pseudonymized even though it does not have an approved suffix. | P1 | Anchor the expression: `.*_(... )$`, and add a fixture for `ssn_hash_backup`. |
| 6 | skills/safe-db-access/templates/postgres/frag_revoke_default_acl.sql:1 | `ALTER DEFAULT PRIVILEGES` without `FOR ROLE` changes only the executing DBA’s defaults, not the source-table creator’s defaults. Future raw tables can therefore retain grants to the AI role. PostgreSQL also distinguishes global and per-schema defaults. | P1 | Capture grantor and object-type/default scope, emit `FOR ROLE <creator>`, and handle global defaults; otherwise mark the plan incomplete. [PostgreSQL docs](https://www.postgresql.org/docs/16/sql-alterdefaultprivileges.html) |
| 7 | skills/safe-db-access/templates/snowflake/35_masking_policy.sql:7 | One `VAL STRING RETURNS STRING` policy is attached to every Restricted column. Common Restricted fields such as `dob` or numeric account identifiers can be DATE/NUMBER, causing the apply to fail because policy input/output types must match the target column. | P1 | Capture Snowflake column types and create/attach type-matched policies, or only emit policy attachments for validated STRING columns and flag the rest incomplete. [Snowflake docs](https://docs.snowflake.com/en/user-guide/security-column-ddm-use?regcode=SNOWSUMMIT400) |
| 8 | skills/safe-db-access/scripts/plan.py:151 | Curated view names use only `<table>`. Two readable tables such as `sales.customers` and `support.customers` silently overwrite one another with `CREATE OR REPLACE VIEW curated.customers`; the plan loses one dataset. | P1 | Refuse duplicate table basenames or generate deterministic, collision-free curated names that include the source schema. |
| 9 | skills/safe-db-access/templates/postgres/frag_revoke_public.sql:1 | The planner automatically revokes **all** PUBLIC privileges, affecting every application role, while rollback restores only `SELECT`. It can break unrelated consumers and is not reversible. | P1 | Do not auto-emit PUBLIC revocations; present them as a separately confirmed manual action and restore the exact captured privilege/grantor state if approved. |
| 10 | skills/safe-db-access/templates/postgres/70_rollback.sql:4 | Rollback is not a state restore: `DROP OWNED BY` drops every object owned by that role in the database and revokes all its privileges. It is unsafe for an existing/custom owner role. | P1 | Require a newly created, planner-scoped owner role, record created object identities, and drop only those objects/privileges. [PostgreSQL docs](https://www.postgresql.org/docs/16/sql-drop-owned.html) |
| 11 | skills/safe-db-access/scripts/plan.py:147 | A Postgres table whose every column is Restricted renders `SELECT` followed only by comments and `FROM`, which is invalid SQL. | P2 | Detect an empty projection and render an explicit incomplete placeholder rather than executable invalid SQL. |
| 12 | skills/safe-db-access/templates/snowflake/60_validate.sql:4 | Snowflake validation is comments plus intentionally failing statements; it has no assertion harness, and executing it normally stops at the first expected error. CI only golden-tests Snowflake text, so it does not prove the claimed access controls. | P2 | Add a Snowflake integration harness or a controlled scripting wrapper that records and asserts expected success/failure for every statement. |

Correct parts:

- The Postgres plan correctly uses a fixed `SECURITY DEFINER` function with an explicit `search_path`; ordinary views use owner privileges unless `security_invoker` is enabled. [PostgreSQL docs](https://www.postgresql.org/docs/16/rules-privileges.html)
- `SERVICE_AGENT`, `IS_AGENT_ACTIVATED()`, `CURRENT_SECONDARY_ROLES()`, scalar SQL-UDF query bodies, and `REVOKE ... ON FUTURE TABLES` are supported Snowflake concepts/syntax. [Agent identity](https://docs.snowflake.com/en/release-notes/2026/other/2026-07-23-service-agent-user-type), [SQL UDFs](https://docs.snowflake.com/en/developer-guide/udf/sql/udf-sql-scalar-functions), [REVOKE](https://docs.snowflake.com/en/sql-reference/sql/revoke-privilege)
- The generated text correctly states that hashing is pseudonymization, not anonymization; NIST likewise warns that hashing direct identifiers carries special risks. [NIST SP 800-188](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-188.3pd.pdf)
```
