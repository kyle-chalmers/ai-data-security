# v0.11.0 Codex review gate (Lake Formation pack)

Independent read-only review by Codex CLI (`codex exec`, live web search) of the full diff against
`origin/main`, with SPEC.md and finding-format.md. Prompt and raw output in the session scratchpad
(`codex-gate-v011/`). Ten findings; all ten addressed before the PR was opened.

| # | Finding (Codex) | Severity | Disposition |
|---|---|---|---|
| 1 | LF-tag policy / LF-tag / tag-expression / conditional grants silently ignored; effective access is the union with named-resource grants | P0 | **Fixed**: any such grant on the principal (or via everyone-principals) → one DB-06 listing them; their tables are never counted. Fixture case added. Citation `lakeformation-tbac` verified live |
| 2 | A DataCellsFilter grant counted as protection regardless of permission; filter-based SELECT not added to readable columns | P0 | **Fixed**: protection requires SELECT on the filter AND the filter definition in the capture; filter SELECT now yields the filter's columns (inclusion or exclusion) in DB-02/03; a table also read in full through another grant is never "protected". Citation `lakeformation-data-filter-grant` |
| 3 | `HybridAccessEnabled` alone reported as an IAM bypass for the principal; opt-ins not captured | P0 | **Fixed**: new optional `opt_ins.json` (`list-lake-formation-opt-ins`); tables mapped to hybrid prefixes via Glue `Location`; DB-07 names only tables where this principal is not opted in (catalog / database / table opt-in honoured); no opt-ins → DB-06, never a DB-07 claim. Citation `lakeformation-hybrid-prerequisites` |
| 4 | `{}` / `{"DataLakeSettings": []}` accepted as settings with zero admins | P0 | **Fixed**: `DataLakeSettings` must be a mapping with list-typed admin/default fields; otherwise DB-06 and DB-05/07(settings)/09 are suppressed. Fixture case added |
| 5 | Empty `AttachedPolicies` gave neither DB-ID-01 nor DB-06 despite unexamined inline / group policies | P0 | **Fixed**: the by-name DB-06 (inline, group, permission boundaries, SCPs, bucket policies not expanded) is always emitted when the capture is present; wording says "AWS-managed policy by its published document" |
| 6 | `SUPER_USER` not in READ / WRITE sets | P0 | **Fixed**: added; catalog-level READ expands to every captured table (or `*.*` when no Glue capture); DB-02 states "SUPER_USER on the catalog". Fixture case added |
| 7 | `jq -s 'add'` keeps one `TableList` | P0 | **Fixed**: recipe, module and SKILL now use `jq -s '{TableList: [.[].TableList[]]}'`; fixture holds two databases and the SUPER_USER case proves the second reaches evaluation |
| 8 | `AWSGlueConsoleFullAccess` / `AWSLakeFormationDataAdmin` classified as CRITICAL S3 bypass | P0 | **Fixed**: removed from the classifier; they fall through to the by-name DB-06 (fixture asserts no DB-ID-01 for AWSGlueConsoleFullAccess) |
| 9 | CloudTrail conclusions without consuming a trail capture; GetDataAccess over-generalised | P1 | **Fixed**: optional `cloudtrail.json` (`describe-trails`) drives DB-05 (INFO with trail count, HIGH with none, DB-06 when absent); DB-05/09 wording: registered locations only, Redshift federated catalogs emit none, S3 data-event enablement not captured. Citation `lakeformation-cloudtrail-logging` verified live |
| 10 | CHANGELOG "the only column control" | P2 | **Fixed** wording |

Correct parts confirmed by Codex: stdlib-only, no AWS commands executed, PrincipalResourcePermissions /
TableWildcard / ColumnNames / ExcludedColumnNames parsing matches the API, names never values.

Local verification after fixes: `tests/test_matchers.py` PASS, `tests/run-fixture-checks.sh` PASS
(lakeformation step + 8 ad-hoc fail-closed cases), `dev/validate.sh` PASS.
