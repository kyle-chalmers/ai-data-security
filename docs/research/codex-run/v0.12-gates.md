# v0.12.0 Codex review gate (DuckDB posture pack)

Independent read-only review by Codex CLI (`codex exec`, live web search) of the full diff against
`origin/main`, with SPEC.md and finding-format.md. Prompt and raw output in the session scratchpad
(`codex-gate-v012/`). Ten findings: eight fixed, one fixed before the gate (citation), one refuted
by a live reproduction.

| # | Finding (Codex) | Severity | Disposition |
|---|---|---|---|
| 1 | DB-05 "no query audit trail" emitted unconditionally; DuckDB 1.4 has `enable_logging` with a `QueryLog` type | P0 | **Fixed**: settings.sql now captures `enable_logging`, `enabled_log_types`, `logging_storage`, `log_query_path`; DB-05 is INFO/probable when QueryLog goes to a file (or the legacy `log_query_path` is set) and MEDIUM/probable otherwise, always stating that logging is per-process and set by the agent's own process |
| 2 | The capture is `-readonly`, so `duckdb_databases().readonly` / `access_mode` describe the audit session, not the agent; a read-write agent could receive the "hardened" DB-01 pass | P0 | **Fixed**: the open-mode inference is gone; a standing DB-06 says the agent's open mode is not observable; DB-01 rests on mode bits only (world-writable CRITICAL, world/group-readable HIGH); DB-ID-01 is INFO only for owner-only files |
| 3 | `allowed_directories` / `allowed_paths` captured but ignored; `allow_persistent_secrets` and `disabled_filesystems` not evaluated | P0 | **Fixed**: DB-09 names allowlisted locations as exceptions to review, flags `allow_persistent_secrets` on, and an empty `disabled_filesystems` while external access is on |
| 4 | DB-10 claims an active path out even when `enable_external_access=false` is locked | P0 | **Fixed**: with external access off, configuration locked and no allowlists, DB-10 becomes a MEDIUM/probable "residual" finding (extensions installed, secrets at rest) that says the agent's process must match; never silent |
| 5 | PII regex used underscore boundaries only; `"member ssn"` missed | P0 | **Fixed**: `(^|[_ ])…([_ ]|$)` in pii_columns.sql; fixture and live check (DuckDB 1.4.1, quoted `member ssn` / `customer email` columns) prove it |
| 6 | Malformed `file.csv` (bogus mode, header-only) failed open | P0 | **Fixed**: exactly one row, octal mode, integer size required; otherwise DB-06 and no DB-01 / DB-ID-01 / DB-08. Fixture case added |
| 7 | A substring (`md5`, `hash`, `substr`…) treated as a masking control, suppressing DB-04 | P0 | **Fixed**: a signal now yields a DB-04 UNKNOWN naming the views, never a pass; DB-04 MEDIUM only when no view has any such expression and raw PII exists |
| 8 | Every persistent secret described as living under `secret_directory`; `storage` is a backend | P1 | **Fixed**: grouped by `storage`; `local_file` → unencrypted files under `secret_directory`, anything else named as its backend without a path claim. Fixture case added |
| 9 | "The empty-CSV premise is incorrect: 1.4.1 emits the header for an empty result" | P2 | **Refuted**: reproduced zero bytes with DuckDB v1.4.1 (Andium) for `select 1 as a where false` both via stdin (`duckdb -csv -header f < q.sql`) and `-c`; the sentinel stays and the README records the observation |
| 10 | `duckdb-cli-arguments` citation claimed the arguments page covers safe mode | P2 | **Fixed** before the gate finished: display text narrowed; new `duckdb-cli-safe-mode` citation (Safe Mode page, fetched live) attached to DB-09 |

Correct parts confirmed by Codex: metadata table functions and columns valid, `-readonly` is the right
capture mode, `secret_string` never selected, default-setting claims accurate, `md:` recognised with
MotherDuck controls left unverified.

Local verification after fixes: six pack queries run live against DuckDB 1.4.1 on a scratch database
and the evaluator judged that capture correctly; `tests/test_matchers.py` PASS,
`tests/run-fixture-checks.sh` PASS (duckdb step + hardened, world-writable, sentinel, logging /
masking / storage, egress-locked, malformed-stat, missing-input cases), `dev/validate.sh` PASS.
