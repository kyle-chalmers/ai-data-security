# Codex review gate — v0.9.0 diff (2026-09-13)

Independent adversarial review of the v0.9 diff (`git diff origin/main -- skills reference tests CHANGELOG.md`, 70 KB) by Codex CLI (`codex exec`, read-only sandbox, live web search) against SPEC.md and finding-format.md. Twelve issues, no P0; eleven fixed in the same PR, one (authorized views) fixed as a fail-closed UNKNOWN because the relationship is not capturable from the dataset side without a further API call.

## Disposition

| # | Codex finding | Sev | Disposition |
|---|---|---|---|
| 1 | Basic roles (Owner/Editor/Viewer) treated as table read/write; they act through dataset default ACLs | P1 | Fixed: basic roles removed from read/write sets; reported as DB-ID-01 breadth (Owner/Editor → CRITICAL) plus a DB-06 naming the dataset-ACL gap. Fixture test: the project Owner produces no DB-02 claim |
| 2 | Conditional bindings evaluated as unconditional and emitted as CRITICAL/HIGH | P1 | Fixed: bindings with a `condition` are excluded from effective access and reported as DB-06 with the condition title. Fixture adds a conditional dataOwner; test proves it is absent from DB-01 |
| 3 | IAM deny policies / principal access boundaries never considered | P1 | Fixed: `deny_policies.json` capture (`gcloud iam policies list --kind=denypolicies`); any deny policy → DB-06 "allow-derived findings may overstate"; PAB and folder/org policies → permanent DB-06; project-role evidence says "subject to deny policies not captured". Tests for both the empty and the non-empty case |
| 4 | Group/domain/principalSet bindings in OBJECT_PRIVILEGES silently ignored | P1 | Fixed: every explicit binding to an indirect principal form is a DB-06 naming object, role, and grantee. Fixture adds a table-level group binding |
| 5 | Capture loop suppressed per-table failures | P1 | Fixed: README recipe runs under `set -euo pipefail`, one file per table, plus a `bq ls` manifest; the evaluator compares the manifest to captured rows and emits DB-06 "capture incomplete" naming missing tables. Test |
| 6 | Cache-hit count covered every job in the project | P1 | Fixed: `audit_logging.sql` filters JOBS to the principal's email (`${principal_email}`), evidence says "the principal's N jobs (billed to this project)" |
| 7 | Only top-level columns scanned; nested STRUCT PII and tags missed | P1 | Fixed: `pii_columns.sql` uses `COLUMN_FIELD_PATHS` (`field_path`); the policy-tag jq recurses into `fields`. Fixture adds `contact.email` / `contact.phone` |
| 8 | Data-governance tags / data policies not recognised → false HIGH | P1 | Fixed: `policy_tags.csv` carries `data_policies`; data-policy-governed fields are excluded from "untagged" and get a DB-06 on the effective masking rule |
| 9 | Project-level Fine-Grained Reader inference is not how the role works | P2 | Fixed: warning removed; DB-06 says FGR is granted at the policy-tag level and taxonomy IAM was not captured |
| 10 | View access not modelled; authorized views not captured | P1 | Fixed (fail closed): grants on VIEW objects are excluded from PII exposure and produce a DB-06 naming the views and the authorized-view gap; capturing `access[].view` per source dataset is documented as the follow-up |
| 11 | `sa_keys.json` mandatory for every principal | P2 | Fixed: `OPTIONAL_JSON_INPUTS` in the loader; the module emits DB-06 for a missing `sa_keys.json` only when the principal is a service account |
| 12 | JOBS precondition omitted `bigquery.jobs.create` | P2 | Fixed in README, SKILL.md, and the SQL comment |

Codex's "correct parts" (OBJECT_PRIVILEGES qualifier/WHERE forms; TABLES types; `COUNTIF`/`creation_time`; policy-tag + dataset-permission model) stand.

## Evidence after fixes
- `tests/test_matchers.py` PASS; `tests/run-fixture-checks.sh` PASS (BigQuery step now 23 assertions); `./dev/validate.sh` PASS in docker.

## Raw output

```
| # | file:line | issue | severity P0/P1/P2 | fix |
|---:|---|---|---|---|
| 1 | `scripts/dialects/bigquery.py:410-415` | Treats project `roles/viewer`, `roles/editor`, and `roles/owner` as unconditional read/write access to every existing dataset/table. BigQuery documents these legacy roles as receiving dataset `READER`/`WRITER`/`OWNER` defaults when datasets are created; `Owner` is explicitly not BigQuery Admin. This produces CRITICAL/HIGH false positives. | P1 | Model these basic roles separately: report their broad project/admin risk, but do not infer access to every existing table without the dataset ACL/default-special-group evidence. |
| 2 | `scripts/dialects/bigquery.py:473-482,513-536` | Conditional allow bindings are deliberately evaluated as unconditional, then emitted as CRITICAL/HIGH findings. IAM grants a conditional binding only when its condition evaluates true, so this is not “fail closed”; it is a severity-bearing false positive. | P1 | Exclude conditional bindings from effective grants and emit DB-06 UNKNOWN, or evaluate a tightly bounded condition deterministically. [IAM Conditions](https://cloud.google.com/iam/docs/conditions-overview) |
| 3 | `scripts/dialects/bigquery.py:467-574` | Never captures or reports IAM deny policies / principal access boundaries. Deny rules override allow roles, so the evaluator can report a principal as able to read or write when the permission is denied. | P1 | Capture applicable project/folder/org deny/PAB policies, or issue a permanent DB-06 and cap affected allow-derived findings. [Deny policies](https://cloud.google.com/iam/docs/deny-overview) |
| 4 | `scripts/dialects/bigquery.py:487-500` | Google Group membership is UNKNOWN only for project-policy bindings. A dataset/table `OBJECT_PRIVILEGES` row for `group:x` is silently ignored for `user:y`, with no DB-06, creating an unmarked false negative. `domain:`, `principal:`, and `principalSet:` bindings have the same problem. | P1 | Scan all explicit grants for indirect principal forms and emit DB-06 whenever membership/set expansion is required. |
| 5 | `sql/bigquery/README.md:750-766` | Capture loops suppress per-table failures: `bq query … \| tail` and `bq ls` run without `set -e -o pipefail`, no manifest is retained, and an errored table query becomes a valid-looking but incomplete `grants.csv`. The evaluator then treats the capture as complete. | P1 | Capture each command to a per-object file, check every exit status under `set -euo pipefail`, validate expected table counts, and emit DB-06 on any failed or missing object capture. |
| 6 | `sql/bigquery/audit_logging.sql:783-789`; `scripts/dialects/bigquery.py:611-621` | Counts cache hits for every job in the project, but labels the result “Audit trail of the AI principal.” This can produce a DB-09 finding entirely due to other users’ queries. | P1 | Filter `JOBS` to the audited principal’s `user_email` after safely deriving/validating it, and state that cross-project billing jobs require separate capture. [JOBS schema and permissions](https://cloud.google.com/bigquery/docs/information-schema-jobs) |
| 7 | `sql/bigquery/pii_columns.sql:840-845`; `sql/bigquery/README.md:763-765` | Only top-level `COLUMNS` and top-level `bq show --schema` fields are scanned. Nested `STRUCT` PII and nested policy tags are missed; Google exposes these through `COLUMN_FIELD_PATHS`. | P1 | Use `INFORMATION_SCHEMA.COLUMN_FIELD_PATHS` for PII/tag analysis, or recursively traverse schema `fields`; preserve full field paths in the recorded data. [COLUMN_FIELD_PATHS](https://cloud.google.com/bigquery/docs/information-schema-column-field-paths) |
| 8 | `scripts/dialects/bigquery.py:546-574` | Treats only legacy Data Catalog `policyTags` as governance. BigQuery also supports Resource Manager data-governance tags and data policies; a protected column using that mechanism is reported as untagged DB-08 HIGH, with no UNKNOWN. | P1 | Capture `dataGovernanceTagsInfo` / `data_policies` from `COLUMNS` or `COLUMN_FIELD_PATHS`, and emit DB-06 until their effective policy is evaluated. [BigQuery governance tags](https://cloud.google.com/bigquery/docs/tags) |
| 9 | `scripts/dialects/bigquery.py:416,562,568-574` | Claims a project-level `roles/datacatalog.categoryFineGrainedReader` grants raw access to every policy tag. Google documents Fine-Grained Reader as applying at the policy-tag level, so the warning is not a valid project-IAM inference. | P2 | Remove the project-level FGR test; capture IAM for each referenced policy-tag taxonomy/tag and evaluate inheritance there. [Column-level access-control roles](https://cloud.google.com/bigquery/docs/column-level-security-intro) |
| 10 | `scripts/dialects/bigquery.py:525-589` | View access is not modeled. The pack neither captures authorized-view/authorized-dataset relationships nor distinguishes normal views; grants on views can therefore cause raw-PII findings despite masked projection, while authorized-view paths are not reliably assessed. | P1 | Capture source-dataset authorized resources and analyze view provenance/definition visibility; otherwise emit DB-06 and cap DB-03/04/08 claims involving views. [Authorized views](https://cloud.google.com/bigquery/docs/authorized-views) |
| 11 | `scripts/dialects/bigquery.py:429-432`; `scripts/eval_grants.py:711-726` | `sa_keys.json` is mandatory for every audit even though the capture instructions say it is only applicable to a service account. User/group audits therefore receive a spurious DB-06 missing-capture UNKNOWN. | P2 | Require and parse `sa_keys.json` only when `--role` is a `serviceAccount:` principal. |
| 12 | `skills/db-access-audit/sql/bigquery/README.md:768-770` | States that `bigquery.jobs.listAll` is the sole JOBS precondition. Google requires both `bigquery.jobs.listAll` and `bigquery.jobs.create`; the wrong gate guidance causes avoidable failed captures. | P2 | Require both permissions in the gate and capture documentation. [JOBS required permissions](https://cloud.google.com/bigquery/docs/information-schema-jobs) |

Correct parts:

- `OBJECT_PRIVILEGES` uses the required region qualifier, explicit columns, and the required dataset/table `WHERE` forms. [Google documentation](https://cloud.google.com/bigquery/docs/information-schema-object-privileges)
- The `TABLES` type handling includes the documented `BASE TABLE`, `CLONE`, `SNAPSHOT`, `VIEW`, `MATERIALIZED VIEW`, and `EXTERNAL` values. [Google documentation](https://cloud.google.com/bigquery/docs/information-schema-tables)
- `COUNTIF(cache_hit)` and the `creation_time` filter are valid GoogleSQL/JOBS fields.
- The policy-tag model correctly recognizes that dataset access plus Fine-Grained Reader is needed for raw protected-column access; taxonomy IAM remains appropriately outside the supplied capture.```
